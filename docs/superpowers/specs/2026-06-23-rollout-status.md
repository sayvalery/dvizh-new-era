# Раскатка — статус (2026-06-23, автономно)

Сделано пока Valery в отъезде. Принцип: всё безопасное — сделано; опасное соло (живая CMS) — подготовлено, доделать вместе (быстро).

## ✅ Сделано, БЕЗОПАСНО, в репо (837df10 + ниже, запушено на GitHub)
- **Бэкапы БД РАБОТАЮТ:** `backup-db.sh` протестирован вживую (дамп 2.3 МБ, 43 заявки, sanity-check OK, медиа-зеркало + копия на VPS). LaunchAgent `com.dvizh.dbbackup` **загружен** (ежедневно 04:30). Вызов бэкапа добавлен перед каждым деплоем в `build-site.sh`.
- **Весь код** интеграций (A+B) / мониторинга / бота — закоммичен и запушен. `payload.config` **валидирован** (`generate:types` + `generate:importmap` прошли — конфиг грузится со всеми новыми коллекциями/глобалами/хуком, синтаксис/структура целы).
- **Фронт собирается** (594 стр.); инъекция page-scripts деградирует мягко без коллекции. Перф починен (фетч скриптов мемоизирован — 1 запрос на сборку вместо 514).
- **Точки отката:** тег `pre-rollout-2026-06-19` + дамп БД `~/dvizh-backups/auto/20260623-155237-pre-rollout/dvizh-db.dump`.
- Токена бота в трекаемом коде НЕТ (только `.env.bot`, gitignored).

## ⏸ НЕ делал соло НАМЕРЕННО (на день запуска = небезопасно / нужны креды) — доделать вместе

### 1. Миграция схемы CMS (~10 мин, под присмотром) — ПОЧЕМУ не соло
CMS работает `next start` (прод, push выключен), **миграционной оснастки нет**. Ломать живую CMS (admin + сборки зависят от неё) в одиночку на день запуска — против «сделай безопасно».
**Изменения чисто АДДИТИВНЫЕ** (новые коллекция `page-scripts` + глобалы `bot-config`/`monitor-status`/`integrations-config`; хук на `FormSubmissions` — БЕЗ новых полей). Безопасная процедура (бэкап уже есть):
1. (бэкап БД уже сделан — см. выше)
2. Временно `push: true` в postgres-адаптере `payload.config` → `docker compose -f docker-compose.prod.yml build cms && up -d cms`.
3. Проверить: админка открывается, блог/контент грузится (нет «column does not exist»), `/admin/status` рендерит, `/api/page-scripts` отвечает.
4. Вернуть `push: false`, пересобрать.
5. Откат при сбое: restore дампа + `git reset --hard pre-rollout-2026-06-19` + rebuild.

### 2. Бот + монитор — нужны креды CMS
Скрипты готовы (`scripts/bot.mjs`, `monitor.mjs`). Для работы:
- Добавить в `.env.bot`: `CMS_EMAIL` + `CMS_PASSWORD` существующего юзера CMS (useAPIKey не добавляли — проще логином).
- Прописать `TELEGRAM_BOT_TOKEN` в env CMS-контейнера (`.env.prod`) — для лид-доставки из хука.
- (опц.) `TELEGRAM_ALERT_CHAT_ID` в `.env.bot` — иначе алерты бэкапа только в лог.
- Установить агенты: `cp scripts/launchagents/com.dvizh.{monitor,bot}.plist ~/Library/LaunchAgents/ && launchctl load -w ...`.
- Бот: твой `/start` уже отправлен → станешь владельцем; затем `/register` в нужном чате.

### 3. Интеграции — заполняешь ты
В админке `integrations-config`: Albato webhook URL, правила по пресетам, `telegramLeadEnabled`.

## Отложено (последним, как договорились)
- Юр «мягкие» пункты (формулировка юриста); переезд на GitLab; запуск домена `.io` (DNS/инфра — только вместе).
- 🔐 **Перевыпустить токен бота** (был прислан в чат) → обновить `.env.bot`.

## Что НЕ трогал из осторожности
- Живую CMS (DB/образ) — см. п.1. Прод-деплой `.cc`/`.io` — не катал соло (инфра/домен). Git-хук guardrail не активировал (влияет на всех в чекауте — включается `git config core.hooksPath .githooks`).
