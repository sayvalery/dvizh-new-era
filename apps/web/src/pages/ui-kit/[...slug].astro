---
import BaseLayout from '../../layouts/BaseLayout.astro'
import NavBar from '../../components/ui/NavBar.astro'
import PersonCard from '../../components/ui/PersonCard.astro'
import Form from '../../components/ui/Form.astro'
import FormClosing from '../../components/ui/FormClosing.astro'
import Drawer from '../../components/ui/Drawer.astro'
import CTABlock from '../../components/blocks/CTABlock.astro'
import ImageBlock from '../../components/blocks/ImageBlock.astro'
import VideoBlock from '../../components/blocks/VideoBlock.astro'
import QuoteBlock from '../../components/blocks/QuoteBlock.astro'
import FormBlock from '../../components/blocks/FormBlock.astro'
import RichTextBlock from '../../components/blocks/RichTextBlock.astro'
import HeroSection from '../../components/ui/HeroSection.astro'
import MeshGradient from '../../components/ui/MeshGradient.astro'
import Timeline from '../../components/ui/Timeline.astro'
import BlogCard from '../../components/ui/BlogCard.astro'
import PropsTable from '../../components/ui/PropsTable.astro'
import BlogCards from '../../components/ui/BlogCards.astro'
import StoryCard from '../../components/ui/StoryCard.astro'
import AdvantageCard from '../../components/ui/AdvantageCard.astro'
import Island from '../../components/ui/Island.astro'
import ModalInlineForm from '../../components/ui/ModalInlineForm.astro'
import DeveloperBlock from '../../components/ui/DeveloperBlock.astro'
import SideMenu from '../../components/ui/SideMenu.astro'
import Button from '../../components/ui/Button.astro'
import Input from '../../components/ui/Input.astro'
import BentoGrid from '../../components/ui/BentoGrid.astro'
import ProductCard from '../../components/ui/ProductCard.astro'
import ProductSection from '../../components/ui/ProductSection.astro'
import FeatureBlock from '../../components/ui/FeatureBlock.astro'
import SuccessStoryBlock from '../../components/ui/SuccessStoryBlock.astro'
import KeyFacts from '../../components/ui/KeyFacts.astro'
import LogoGrid from '../../components/ui/LogoGrid.astro'
import logoGridRaw from '../../components/ui/LogoGrid.astro?raw'
import MegaMenu from '../../components/ui/MegaMenu.astro'
import megaMenuRaw from '../../components/ui/MegaMenu.astro?raw'
import ComparisonSection from '../../components/ui/ComparisonSection.astro'
import Banner from '../../components/ui/Banner.astro'
import CTABanner from '../../components/ui/CTABanner.astro'
import ReviewFull from '../../components/ui/ReviewFull.astro'
import TextImageBlock from '../../components/ui/TextImageBlock.astro'
import SupportSection from '../../components/ui/SupportSection.astro'
import BlogGrid from '../../components/ui/BlogGrid.astro'
import CTA from '../../components/ui/CTA.astro'
import ClearGlassBg from '../../components/ui/ClearGlassBg.astro'
import FlutedGlassBg from '../../components/ui/FlutedGlassBg.astro'
import FunnelSection from '../../components/ui/FunnelSection.astro'
import LogoDrum from '../../components/ui/LogoDrum.astro'
import PageSection from '../../components/ui/PageSection.astro'
import ProductLink from '../../components/ui/ProductLink.astro'
import Review from '../../components/ui/Review.astro'

// --- Исходники компонентов для парсинга пропсов (Vite ?raw import) ---
import navBarRaw from '../../components/ui/NavBar.astro?raw'
import personCardRaw from '../../components/ui/PersonCard.astro?raw'
import formRaw from '../../components/ui/Form.astro?raw'
import formClosingRaw from '../../components/ui/FormClosing.astro?raw'
import heroSectionRaw from '../../components/ui/HeroSection.astro?raw'
import meshGradientRaw from '../../components/ui/MeshGradient.astro?raw'
import timelineRaw from '../../components/ui/Timeline.astro?raw'
import blogCardRaw from '../../components/ui/BlogCard.astro?raw'
import blogCardsRaw from '../../components/ui/BlogCards.astro?raw'
import storyCardRaw from '../../components/ui/StoryCard.astro?raw'
import advantageCardRaw from '../../components/ui/AdvantageCard.astro?raw'
import islandRaw from '../../components/ui/Island.astro?raw'
import developerBlockRaw from '../../components/ui/DeveloperBlock.astro?raw'
import buttonRaw from '../../components/ui/Button.astro?raw'
import inputRaw from '../../components/ui/Input.astro?raw'
import bentoGridRaw from '../../components/ui/BentoGrid.astro?raw'
import productCardRaw from '../../components/ui/ProductCard.astro?raw'
import productSectionRaw from '../../components/ui/ProductSection.astro?raw'
import featureBlockRaw from '../../components/ui/FeatureBlock.astro?raw'
import successStoryBlockRaw from '../../components/ui/SuccessStoryBlock.astro?raw'
import keyFactsRaw from '../../components/ui/KeyFacts.astro?raw'
import comparisonSectionRaw from '../../components/ui/ComparisonSection.astro?raw'
import reviewFullRaw from '../../components/ui/ReviewFull.astro?raw'
import textImageBlockRaw from '../../components/ui/TextImageBlock.astro?raw'
import supportSectionRaw from '../../components/ui/SupportSection.astro?raw'
import blogGridRaw from '../../components/ui/BlogGrid.astro?raw'
import bannerRaw from '../../components/ui/Banner.astro?raw'
import ctaRaw from '../../components/ui/CTA.astro?raw'
import ctaBannerRaw from '../../components/ui/CTABanner.astro?raw'
import clearGlassBgRaw from '../../components/ui/ClearGlassBg.astro?raw'
import flutedGlassBgRaw from '../../components/ui/FlutedGlassBg.astro?raw'
import funnelSectionRaw from '../../components/ui/FunnelSection.astro?raw'
import logoDrumRaw from '../../components/ui/LogoDrum.astro?raw'
import pageSectionRaw from '../../components/ui/PageSection.astro?raw'
import productLinkRaw from '../../components/ui/ProductLink.astro?raw'
import reviewRaw from '../../components/ui/Review.astro?raw'

interface PropMeta {
  name: string
  type: string
  required: boolean
  description: string
  defaultValue?: string
}

function parseSource(source: string): { description: string; props: PropMeta[] } {
  if (!source) return { description: '', props: [] }

  // JSDoc описание (первый блок перед interface Props)
  const jsdocMatch = source.match(/\/\*\*\s*\n([\s\S]*?)\*\//)
  let description = ''
  if (jsdocMatch) {
    description = jsdocMatch[1]
      .replace(/^\s*\*\s?/gm, '')
      .split('\n@')[0]
      .trim()
  }

  // interface Props { ... }
  const propsMatch = source.match(/interface Props\s*\{([\s\S]*?)\n\}/)
  if (!propsMatch) return { description, props: [] }

  const props: PropMeta[] = []
  const lines = propsMatch[1].split('\n')
  let comment = ''

  for (const line of lines) {
    const cmt = line.match(/\/\*\*\s*(.*?)\s*\*\//)
    if (cmt) { comment = cmt[1]; continue }
    const prop = line.match(/^\s*(\w+)(\?)?\s*:\s*(.+)$/)
    if (prop) {
      props.push({
        name: prop[1],
        required: !prop[2],
        type: prop[3].trim().replace(/;?\s*$/, ''),
        description: comment,
      })
      comment = ''
    }
  }

  // Дефолтные значения из деструктуризации
  const destructMatch = source.match(/const\s*\{([\s\S]*?)\}\s*=\s*Astro\.props/)
  if (destructMatch) {
    const block = destructMatch[1]
    const re = /(\w+)(?:\s*:\s*\w+)?\s*=\s*([^,}\n]+)/g
    let m
    while ((m = re.exec(block)) !== null) {
      const p = props.find(x => x.name === m[1])
      if (p) p.defaultValue = m[1] === 'class' ? "''" : m[2].trim()
    }
  }

  return { description, props }
}

// Парсим все компоненты
const navBarMeta = parseSource(navBarRaw)
const personCardMeta = parseSource(personCardRaw)
const formMeta = parseSource(formRaw)
const formClosingMeta = parseSource(formClosingRaw)
const heroSectionMeta = parseSource(heroSectionRaw)
const meshGradientMeta = parseSource(meshGradientRaw)
const timelineMeta = parseSource(timelineRaw)
const blogCardMeta = parseSource(blogCardRaw)
const blogCardsMeta = parseSource(blogCardsRaw)
const storyCardMeta = parseSource(storyCardRaw)
const advantageCardMeta = parseSource(advantageCardRaw)
const islandMeta = parseSource(islandRaw)
const buttonMeta = parseSource(buttonRaw)
const inputMeta = parseSource(inputRaw)
const bentoGridMeta = parseSource(bentoGridRaw)
const productCardMeta = parseSource(productCardRaw)
const productSectionMeta = parseSource(productSectionRaw)
const reviewFullMeta = parseSource(reviewFullRaw)
const textImageBlockMeta = parseSource(textImageBlockRaw)
const featureBlockMeta = parseSource(featureBlockRaw)
const blogGridMeta = parseSource(blogGridRaw)
const bannerMeta = parseSource(bannerRaw)
const ctaMeta = parseSource(ctaRaw)
const ctaBannerMeta = parseSource(ctaBannerRaw)
const clearGlassBgMeta = parseSource(clearGlassBgRaw)
const flutedGlassBgMeta = parseSource(flutedGlassBgRaw)
const funnelSectionMeta = parseSource(funnelSectionRaw)
const logoDrumMeta = parseSource(logoDrumRaw)
const pageSectionMeta = parseSource(pageSectionRaw)
const productLinkMeta = parseSource(productLinkRaw)
const reviewMeta = parseSource(reviewRaw)
const developerBlockMeta = parseSource(developerBlockRaw)
const supportSectionMeta = parseSource(supportSectionRaw)
const megaMenuMeta = parseSource(megaMenuRaw)

// В продакшн-билде страница не генерируется
export function getStaticPaths() {
  if (import.meta.env.PROD) return []
  return [
    { params: { slug: undefined }, props: { section: 'overview' } },
    { params: { slug: 'blocks' }, props: { section: 'blocks' } },
    { params: { slug: 'drawer' }, props: { section: 'drawer' } },
    { params: { slug: 'typography' }, props: { section: 'typography' } },
    { params: { slug: 'colors' }, props: { section: 'colors' } },
    { params: { slug: 'advantage-card' }, props: { section: 'advantage-card' } },
    { params: { slug: 'bento' }, props: { section: 'bento' } },
    { params: { slug: 'blogcard' }, props: { section: 'blogcard' } },
    { params: { slug: 'blog-cards' }, props: { section: 'blog-cards' } },
    { params: { slug: 'blog-grid' }, props: { section: 'blog-grid' } },
    { params: { slug: 'buttons' }, props: { section: 'buttons' } },
    { params: { slug: 'clear-glass-bg' }, props: { section: 'clear-glass-bg' } },
    { params: { slug: 'comparison' }, props: { section: 'comparison' } },
    { params: { slug: 'banner' }, props: { section: 'banner' } },
    { params: { slug: 'cta' }, props: { section: 'cta' } },
    { params: { slug: 'cta-banner' }, props: { section: 'cta-banner' } },
    { params: { slug: 'developer-block' }, props: { section: 'developer-block' } },
    { params: { slug: 'feature-block' }, props: { section: 'feature-block' } },
    { params: { slug: 'fluted-glass-bg' }, props: { section: 'fluted-glass-bg' } },
    { params: { slug: 'forms' }, props: { section: 'forms' } },
    { params: { slug: 'form-closing' }, props: { section: 'form-closing' } },
    { params: { slug: 'funnel-section' }, props: { section: 'funnel-section' } },
    { params: { slug: 'navigation' }, props: { section: 'navigation' } },
    { params: { slug: 'hero' }, props: { section: 'hero' } },
    { params: { slug: 'input' }, props: { section: 'input' } },
    { params: { slug: 'island' }, props: { section: 'island' } },
    { params: { slug: 'key-facts' }, props: { section: 'key-facts' } },
    { params: { slug: 'logo-drum' }, props: { section: 'logo-drum' } },
    { params: { slug: 'logo-grid' }, props: { section: 'logo-grid' } },
    { params: { slug: 'mega-menu' }, props: { section: 'mega-menu' } },
    { params: { slug: 'mesh-gradient' }, props: { section: 'mesh-gradient' } },
    { params: { slug: 'modal-inline-form' }, props: { section: 'modal-inline-form' } },
    { params: { slug: 'page-section' }, props: { section: 'page-section' } },
    { params: { slug: 'persons' }, props: { section: 'persons' } },
    { params: { slug: 'product-card' }, props: { section: 'product-card' } },
    { params: { slug: 'product-section' }, props: { section: 'product-section' } },
    { params: { slug: 'product-link' }, props: { section: 'product-link' } },
    { params: { slug: 'review' }, props: { section: 'review' } },
    { params: { slug: 'side-menu' }, props: { section: 'side-menu' } },
    { params: { slug: 'story-card' }, props: { section: 'story-card' } },
    { params: { slug: 'success-story-block' }, props: { section: 'success-story-block' } },
    { params: { slug: 'support-section' }, props: { section: 'support-section' } },
    { params: { slug: 'text-image-block' }, props: { section: 'text-image-block' } },
    { params: { slug: 'timeline' }, props: { section: 'timeline' } },
  ]
}

const { section } = Astro.props

const sections = [
  { slug: '', label: 'Обзор' },
  { slug: 'blocks', label: 'Блоки статей' },
  { slug: 'drawer', label: 'Дровер' },
  { slug: 'typography', label: 'Типографика' },
  { slug: 'colors', label: 'Цвета' },
  { slug: 'advantage-card', label: 'AdvantageCard' },
  { slug: 'banner', label: 'Banner' },
  { slug: 'bento', label: 'Bento Grid' },
  { slug: 'blogcard', label: 'Blog Card' },
  { slug: 'blog-cards', label: 'Blog Cards' },
  { slug: 'blog-grid', label: 'BlogGrid' },
  { slug: 'buttons', label: 'Button' },
  { slug: 'clear-glass-bg', label: 'ClearGlassBg' },
  { slug: 'comparison', label: 'ComparisonSection' },
  { slug: 'cta', label: 'CTA' },
  { slug: 'cta-banner', label: 'CTABanner' },
  { slug: 'developer-block', label: 'DeveloperBlock' },
  { slug: 'feature-block', label: 'FeatureBlock' },
  { slug: 'fluted-glass-bg', label: 'FlutedGlassBg' },
  { slug: 'forms', label: 'Form' },
  { slug: 'form-closing', label: 'FormClosing' },
  { slug: 'funnel-section', label: 'FunnelSection' },
  { slug: 'navigation', label: 'Header' },
  { slug: 'hero', label: 'Hero Section' },
  { slug: 'input', label: 'Input' },
  { slug: 'island', label: 'Island' },
  { slug: 'key-facts', label: 'KeyFacts' },
  { slug: 'logo-drum', label: 'LogoDrum' },
  { slug: 'logo-grid', label: 'LogoGrid' },
  { slug: 'mega-menu', label: 'MegaMenu' },
  { slug: 'mesh-gradient', label: 'Mesh Gradient' },
  { slug: 'modal-inline-form', label: 'Modal Inline Form' },
  { slug: 'page-section', label: 'PageSection' },
  { slug: 'persons', label: 'PersonCard' },
  { slug: 'product-card', label: 'Product Card' },
  { slug: 'product-section', label: 'Product Section' },
  { slug: 'product-link', label: 'ProductLink' },
  { slug: 'review', label: 'Review' },
  { slug: 'side-menu', label: 'Side Menu' },
  { slug: 'story-card', label: 'StoryCard' },
  { slug: 'success-story-block', label: 'SuccessStoryBlock' },
  { slug: 'support-section', label: 'SupportSection' },
  { slug: 'text-image-block', label: 'TextImageBlock' },
  { slug: 'timeline', label: 'Timeline' },
]
---

<BaseLayout title={`UI Kit — ${section}`}>
  <div class="flex min-h-screen">
    <!-- Sidebar -->
    <aside class="w-56 border-r border-gray-200 px-4 py-8 shrink-0 flex flex-col">
      <p class="text-xs font-semibold uppercase tracking-widest text-gray-400 mb-4">UI Kit</p>
      <nav class="space-y-1 flex-1">
        {sections.map(s => (
          <a
            href={s.slug ? `/ui-kit/${s.slug}` : '/ui-kit'}
            class={`block px-3 py-1.5 rounded text-sm ${section === (s.slug || 'overview') ? 'bg-gray-100 font-medium' : 'text-gray-600 hover:text-gray-900'}`}
          >
            {s.label}
          </a>
        ))}
      </nav>
      <a href="/" class="block px-3 py-1.5 text-sm text-gray-400 hover:text-gray-900 mt-4">← На сайт</a>
    </aside>

    <!-- Content -->
    <main class="flex-1 px-10 py-8">
      {section === 'overview' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">UI Kit</h1>
          <p class="text-gray-600 mb-8">Компоненты и токены дизайн-системы ДВИЖ. Доступно только в dev-режиме.</p>
          <div class="grid grid-cols-2 gap-4 max-w-lg">
            {sections.slice(1).map(s => (
              <a href={`/ui-kit/${s.slug}`} class="p-4 border rounded-xl hover:bg-gray-50 text-sm font-medium">
                {s.label} →
              </a>
            ))}
          </div>
        </div>
      )}

      {section === 'navigation' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Header</h1>
          <PropsTable props={navBarMeta.props} class="mb-8" />

          <div class="space-y-10">
            <div>
              <p class="text-sm font-medium text-gray-400 mb-3">Основной сайт — с дропдаунами</p>
              <div class="-mx-10 border-y border-gray-200">
                <NavBar
                  logo={{ label: 'ДВИЖ', href: '#', image: '/logo-dvizh.svg' }}
                  items={[
                    { label: 'Продукты', children: [
                      { label: 'Ипотека', href: '#', description: 'Ипотечный процесс от заявки до выдачи' },
                      { label: 'Электронная регистрация', href: '#', description: 'Цифровая регистрация сделок' },
                      { label: 'QBR', href: '#', description: 'Аналитика и отчёты' },
                    ]},
                    { label: 'Решения', children: [
                      { label: 'Для банков', href: '#' },
                      { label: 'Для девелоперов', href: '#' },
                    ]},
                    { label: 'Для кого', children: [
                      { label: 'Департамент продаж', href: '#' },
                      { label: 'Отдел ипотеки', href: '#' },
                    ]},
                    { label: 'Блог', href: '#', badge: 'dot' },
                    { label: 'О нас', href: '#' },
                  ]}
                  cta={{ label: 'Запросить демо', preset: 'demo' }}
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-3">Блог / ДВИЖ Инсайты — с вторичной ссылкой</p>
              <div class="-mx-10 border-y border-gray-200">
                <NavBar
                  logo={{ label: 'ДВИЖ Инсайты', href: '#', image: '/logo-dvizh-insights.svg' }}
                  items={[
                    { label: 'Блог', href: '#' },
                    { label: 'Словарь девелопера', href: '#' },
                  ]}
                  secondaryLink={{ label: '← dvizh.io', href: '#' }}
                />
              </div>
            </div>
          </div>
        </div>
      )}

      {section === 'persons' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Персоны</h1>
          <PropsTable props={personCardMeta.props} class="mb-8" />

          <!-- Alpine Playground -->
          <div class="mb-10">
            <p class="text-xs font-semibold uppercase tracking-wider text-gray-400 mb-3">Playground</p>
            <div
              x-data="{
                name: 'Иван Азизов',
                jobTitle: 'CEO, ДВИЖ',
                variant: 'full'
              }"
              class="grid lg:grid-cols-[260px_1fr] border rounded-xl overflow-hidden"
            >
              <!-- Controls -->
              <div class="p-4 bg-gray-50 border-b lg:border-b-0 lg:border-r border-gray-200 space-y-3">
                <div>
                  <label class="text-xs text-gray-500 block mb-1">variant</label>
                  <select x-model="variant" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm bg-white">
                    <option value="full">full</option>
                    <option value="inline">inline</option>
                  </select>
                </div>
                <div>
                  <label class="text-xs text-gray-500 block mb-1">name</label>
                  <input x-model="name" type="text" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm" />
                </div>
                <div>
                  <label class="text-xs text-gray-500 block mb-1">jobTitle</label>
                  <input x-model="jobTitle" type="text" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm" />
                </div>
              </div>

              <!-- Preview -->
              <div class="p-8 bg-white flex items-center justify-center min-h-[120px]">
                <!-- Full variant -->
                <div x-show="variant === 'full'" class="flex items-center gap-3">
                  <div class="w-11 h-11 rounded-full bg-gray-200 shrink-0 flex items-center justify-center">
                    <span class="text-sm text-gray-500 font-medium" x-text="name[0]"></span>
                  </div>
                  <div class="min-w-0">
                    <p class="text-sm font-medium text-gray-900 truncate" x-text="name"></p>
                    <p x-show="jobTitle" class="text-xs text-gray-500 truncate" x-text="jobTitle"></p>
                  </div>
                </div>

                <!-- Inline variant -->
                <div x-show="variant === 'inline'" class="flex items-center gap-2">
                  <div class="w-6 h-6 rounded-full bg-gray-200 shrink-0 flex items-center justify-center">
                    <span class="text-xs text-gray-500 font-medium" x-text="name[0]"></span>
                  </div>
                  <span class="text-sm text-gray-700" x-text="name"></span>
                </div>
              </div>
            </div>
          </div>

          <div class="space-y-10">

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Full — аватар + имя + должность</p>
              <div class="flex flex-wrap gap-6">
                <PersonCard person={{ name: 'Иван Азизов', jobTitle: 'CEO, ДВИЖ' }} />
                <PersonCard person={{ name: 'Анна Смирнова', jobTitle: 'Head of Product' }} />
                <PersonCard person={{ name: 'Без должности' }} />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Inline — аватар + имя (без подзаголовка)</p>
              <div class="flex flex-wrap gap-4">
                <PersonCard person={{ name: 'Иван Азизов', jobTitle: 'CEO', photo: { url: 'https://i.pravatar.cc/40?img=11' } }} variant="inline" />
                <PersonCard person={{ name: 'Анна Смирнова', photo: { url: 'https://i.pravatar.cc/40?img=5' } }} variant="inline" />
                <PersonCard person={{ name: 'Без фото' }} variant="inline" />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">В контексте статьи — inline в шапке, full внизу</p>
              <div class="border rounded-xl p-6 max-w-lg space-y-4">
                <div class="flex items-center gap-4">
                  <PersonCard person={{ name: 'Иван Азизов', photo: { url: 'https://i.pravatar.cc/40?img=11' } }} variant="inline" />
                  <span class="text-sm text-gray-400">12 января 2025</span>
                </div>
                <p class="text-sm text-gray-600">Текст статьи…</p>
                <div class="border-t pt-4">
                  <p class="text-xs uppercase tracking-wide text-gray-400 mb-3">Автор</p>
                  <PersonCard person={{ name: 'Иван Азизов', jobTitle: 'CEO, ДВИЖ' }} variant="full" />
                </div>
              </div>
            </div>

          </div>
        </div>
      )}

      {section === 'typography' && (
        <div class="space-y-12">
          <h1 class="text-3xl font-bold font-heading mb-6">Типографика</h1>

          <!-- Заголовки -->
          <div>
            <p class="text-xs text-gray-400 uppercase tracking-widest mb-6">Заголовки — Styrene A</p>
            <div class="space-y-8">
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-h1 — Styrene Bold 64, lh 100%, ls -1px</span>
                <p class="text-h1">H1 Bold 64</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-h1-italic — Styrene Bold Italic 64, lh 100%, ls -1px</span>
                <p class="text-h1-italic">H1 Bold Italic 64</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-h2 — Styrene Medium 48, lh 100%, ls -2px</span>
                <p class="text-h2">H2 Medium 48</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-h3 — Styrene Medium 32, lh 100%, ls -1px</span>
                <p class="text-h3">H3 Medium 32</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-h4 — Styrene Medium 24, lh 120%, ls -2px</span>
                <p class="text-h4">H4 Medium 24</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-h5 — Styrene Bold 18, lh 120%, ls -2px</span>
                <p class="text-h5">H5 Bold 18</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-h6 — Styrene Bold 16, lh 120%, ls -2px</span>
                <p class="text-h6">H6 Bold 16</p>
              </div>
            </div>
          </div>

          <!-- Тело текста -->
          <div>
            <p class="text-xs text-gray-400 uppercase tracking-widest mb-6">Тело текста — Inter</p>
            <div class="space-y-8">
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-medium-32 — Inter Medium 32, lh 100%, ls -1px</span>
                <p class="text-medium-32">Medium 32</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-body-lg — Inter Regular 18, lh 140%, ls -0.5px</span>
                <p class="text-body-lg">Body 18. ДВИЖ помогает застройщикам автоматизировать продажи и ипотеку.</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-body-lg-italic — Inter Regular Italic 18, lh 140%, ls -0.5px</span>
                <p class="text-body-lg-italic">Body Italic 18. ДВИЖ помогает застройщикам автоматизировать продажи и ипотеку.</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-subhead — Inter Regular 16, lh 140%, ls -0.5px</span>
                <p class="text-subhead">Subhead 16. Вспомогательный текст и подзаголовки.</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-footnote — Inter Regular 14, lh 140%, ls -0.5px</span>
                <p class="text-footnote">Footnote 14. Мелкие подписи, метаданные, примечания.</p>
              </div>
              <div>
                <span class="text-xs text-gray-400 block mb-2">.text-side-menu — Inter SemiBold 12, lh 140%, ls 1px, uppercase</span>
                <p class="text-side-menu">Side Menu 12</p>
              </div>
            </div>
          </div>
        </div>
      )}

      {section === 'colors' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-8">Цвета</h1>
          <div class="space-y-10">

            <!-- Brand -->
            <div>
              <p class="text-sm font-semibold mb-4">Бренд (Brand) — #ff4d00</p>
              <div class="flex flex-wrap gap-3">
                {[
                  { cls: 'bg-brand-50',  label: '50',  hex: '#fff5ed' },
                  { cls: 'bg-brand-100', label: '100', hex: '#ffe8d5' },
                  { cls: 'bg-brand-200', label: '200', hex: '#fecda9' },
                  { cls: 'bg-brand-300', label: '300', hex: '#fda973' },
                  { cls: 'bg-brand-400', label: '400', hex: '#fb7a3a' },
                  { cls: 'bg-brand',     label: '500',  hex: '#ff4d00' },
                  { cls: 'bg-brand-600', label: '600', hex: '#e64600' },
                  { cls: 'bg-brand-700', label: '700', hex: '#be3500' },
                  { cls: 'bg-brand-800', label: '800', hex: '#972c07' },
                  { cls: 'bg-brand-900', label: '900', hex: '#7a280c' },
                ].map(s => (
                  <div class="flex flex-col items-center gap-1.5">
                    <div class={`w-16 h-16 rounded-xl border border-black/5 ${s.cls}`}></div>
                    <span class="text-xs font-medium text-gray-700">{s.label}</span>
                    <span class="text-[10px] text-gray-400 font-mono">{s.hex}</span>
                  </div>
                ))}
              </div>
            </div>

            <!-- Gray -->
            <div>
              <p class="text-sm font-semibold mb-4">Серый (Gray)</p>
              <div class="flex flex-wrap gap-3">
                {[
                  { cls: 'bg-gray-100', label: '100', hex: '#FBF9F6' },
                  { cls: 'bg-gray-200', label: '200', hex: '#F3F1EE' },
                  { cls: 'bg-gray-300', label: '300', hex: '#E6E4E1' },
                  { cls: 'bg-gray-400', label: '400', hex: '#C2C2C2' },
                  { cls: 'bg-gray-500', label: '500', hex: '#858585' },
                  { cls: 'bg-gray-600', label: '600', hex: '#6D6C6C' },
                  { cls: 'bg-gray-700', label: '700', hex: '#505050' },
                  { cls: 'bg-gray-800', label: '800', hex: '#323232' },
                  { cls: 'bg-gray-900', label: '900', hex: '#1D1E21' },
                ].map(s => (
                  <div class="flex flex-col items-center gap-1.5">
                    <div class={`w-16 h-16 rounded-xl border border-black/5 ${s.cls}`}></div>
                    <span class="text-xs font-medium text-gray-700">{s.label}</span>
                    <span class="text-[10px] text-gray-400 font-mono">{s.hex}</span>
                  </div>
                ))}
              </div>
            </div>

            <!-- Base -->
            <div>
              <p class="text-sm font-semibold mb-4">Базовые</p>
              <div class="flex flex-wrap gap-3">
                {[
                  { style: 'background:#ffffff', label: 'white',          hex: '#ffffff',  border: true },
                  { style: 'background:#000000', label: 'black',          hex: '#000000'               },
                  { style: 'background:#FBF9F6', label: 'background',     hex: '#FBF9F6',  border: true },
                  { style: 'background:#1D1E21', label: 'text-primary',   hex: '#1D1E21'               },
                  { style: 'background:#858585', label: 'text-secondary', hex: '#858585'               },
                ].map(s => (
                  <div class="flex flex-col items-center gap-1.5">
                    <div class={`w-16 h-16 rounded-xl ${s.border ? 'border border-gray-200' : ''}`} style={s.style}></div>
                    <span class="text-xs font-medium text-gray-700">{s.label}</span>
                    <span class="text-[10px] text-gray-400 font-mono">{s.hex}</span>
                  </div>
                ))}
              </div>
            </div>

          </div>
        </div>
      )}

      {section === 'buttons' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">Кнопки</h1>
          <p class="text-gray-500 text-sm mb-2">4 варианта · 3 размера · 3 состояния · иконки с обеих сторон</p>
          <p class="text-xs text-gray-400 font-mono mb-8">components/ui/Button.astro</p>

          <PropsTable props={buttonMeta.props} class="mb-12" />

          <!-- Размеры и варианты (статика) -->
          {[
            {
              variant: 'primary' as const,
              desc: 'Оранжевый фон, белый текст',
              states: {
                default:  'bg-brand text-white',
                hover:    'bg-brand-600 text-white',
                disabled: 'bg-brand text-white opacity-40',
              },
            },
            {
              variant: 'secondary' as const,
              desc: 'Белый фон, тёмный текст',
              states: {
                default:  'bg-white text-[#122226]',
                hover:    'bg-[#e6e4e1] text-[#122226]',
                disabled: 'bg-white text-[#9ca3a8] opacity-60',
              },
            },
            {
              variant: 'ghost' as const,
              desc: 'Прозрачный фон, тёмный текст',
              states: {
                default:  'text-[#122226]',
                hover:    'bg-[#e6e4e1] text-[#122226]',
                disabled: 'text-[#6b7280] opacity-60',
              },
            },
            {
              variant: 'dark' as const,
              desc: 'Тёмный фон, белый текст',
              states: {
                default:  'bg-[#1d1e21] text-white',
                hover:    'bg-[#323232] text-white',
                disabled: 'bg-[#1d1e21] text-white opacity-40',
              },
            },
          ].map(group => (
            <div class="mb-12">
              <h2 class="text-base font-semibold mb-0.5 capitalize">{group.variant}</h2>
              <p class="text-xs text-gray-500 mb-5">{group.desc}</p>

              <div class="grid grid-cols-[64px_1fr_1fr_1fr] gap-x-6 mb-2 px-1">
                <div></div>
                <span class="text-xs text-gray-400 font-medium">Default</span>
                <span class="text-xs text-gray-400 font-medium">Hover</span>
                <span class="text-xs text-gray-400 font-medium">Disabled</span>
              </div>

              <div class="space-y-3">
                {(['sm', 'md', 'lg'] as const).map(size => {
                  const pad = size === 'sm' ? 'px-4 py-2 h-9' : size === 'md' ? 'px-6 py-3 h-11' : 'px-8 py-4 h-[52px]'
                  const txt = size === 'lg' ? 'text-btn-lg' : 'text-btn'
                  return (
                    <div class="grid grid-cols-[64px_1fr_1fr_1fr] gap-x-6 items-center">
                      <span class="text-xs text-gray-400 uppercase">{size}</span>
                      <div>
                        <button class={`inline-flex items-center justify-center rounded-[12px] ${pad} ${txt} ${group.states.default}`}>Button</button>
                      </div>
                      <div>
                        <button class={`inline-flex items-center justify-center rounded-[12px] ${pad} ${txt} ${group.states.hover}`}>Button</button>
                      </div>
                      <div>
                        <button class={`inline-flex items-center justify-center rounded-[12px] ${pad} ${txt} ${group.states.disabled} cursor-not-allowed`} disabled>Button</button>
                      </div>
                    </div>
                  )
                })}
              </div>
            </div>
          ))}

          <!-- Живые кнопки (реальный hover) -->
          <div class="pt-8 border-t border-gray-200 mb-12">
            <p class="text-xs text-gray-400 uppercase tracking-widest mb-5">Живые — реальный hover через Button.astro</p>
            <div class="flex flex-wrap gap-3 items-center">
              <Button size="md" variant="primary">Primary</Button>
              <Button size="md" variant="secondary">Secondary</Button>
              <Button size="md" variant="ghost">Ghost</Button>
              <Button size="md" variant="dark">Dark</Button>
            </div>
            <div class="flex flex-wrap gap-3 items-center mt-3 bg-gray-900 rounded-xl p-4">
              <p class="text-xs text-white/40 w-full mb-1">На тёмном фоне</p>
              <Button size="md" variant="ghost-dark">Ghost Dark</Button>
            </div>
            <div class="flex flex-wrap gap-3 items-center mt-3">
              <Button size="sm" variant="primary">Small</Button>
              <Button size="md" variant="primary">Medium</Button>
              <Button size="lg" variant="primary">Large</Button>
            </div>
          </div>

          <!-- Иконки -->
          <div class="pt-8 border-t border-gray-200">
            <p class="text-xs text-gray-400 uppercase tracking-widest mb-2">Иконки</p>
            <p class="text-xs text-gray-400 mb-6">Слоты <code class="bg-gray-100 px-1 rounded">icon-left</code> и <code class="bg-gray-100 px-1 rounded">icon-right</code>. Размер: SM→16px, MD→18px, LG→20px. Отступ→4px.</p>

            <div class="space-y-6">
              <!-- Иконка слева -->
              <div>
                <p class="text-xs text-gray-500 mb-3">Иконка слева</p>
                <div class="flex flex-wrap gap-3 items-center">
                  <Button size="sm" variant="primary">
                    <svg slot="icon-left" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                      <circle cx="8" cy="8" r="6.5" stroke="currentColor" stroke-width="1.5"/>
                      <path d="M8 5v3.5l2 2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                    </svg>
                    SM — иконка слева
                  </Button>
                  <Button size="md" variant="primary">
                    <svg slot="icon-left" viewBox="0 0 18 18" fill="none" aria-hidden="true">
                      <circle cx="9" cy="9" r="7.5" stroke="currentColor" stroke-width="1.5"/>
                      <path d="M9 5.5v4l2.5 2.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                    </svg>
                    MD — иконка слева
                  </Button>
                  <Button size="lg" variant="primary">
                    <svg slot="icon-left" viewBox="0 0 20 20" fill="none" aria-hidden="true">
                      <circle cx="10" cy="10" r="8.5" stroke="currentColor" stroke-width="1.5"/>
                      <path d="M10 6v4.5l3 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                    </svg>
                    LG — иконка слева
                  </Button>
                </div>
              </div>

              <!-- Иконка справа -->
              <div>
                <p class="text-xs text-gray-500 mb-3">Иконка справа</p>
                <div class="flex flex-wrap gap-3 items-center">
                  <Button size="sm" variant="secondary">
                    SM — иконка справа
                    <svg slot="icon-right" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                      <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </Button>
                  <Button size="md" variant="secondary">
                    MD — иконка справа
                    <svg slot="icon-right" viewBox="0 0 18 18" fill="none" aria-hidden="true">
                      <path d="M3.5 9h11M10 4.5l4.5 4.5-4.5 4.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </Button>
                  <Button size="lg" variant="secondary">
                    LG — иконка справа
                    <svg slot="icon-right" viewBox="0 0 20 20" fill="none" aria-hidden="true">
                      <path d="M4 10h12M11 5l5 5-5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </Button>
                </div>
              </div>

              <!-- Иконки с обеих сторон -->
              <div>
                <p class="text-xs text-gray-500 mb-3">Иконки с обеих сторон</p>
                <div class="flex flex-wrap gap-3 items-center">
                  <Button size="sm" variant="dark">
                    <svg slot="icon-left" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                      <path d="M8 2a6 6 0 100 12A6 6 0 008 2zm0 3v3.5l2 1.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                    </svg>
                    SM
                    <svg slot="icon-right" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                      <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </Button>
                  <Button size="md" variant="dark">
                    <svg slot="icon-left" viewBox="0 0 18 18" fill="none" aria-hidden="true">
                      <path d="M9 2.25a6.75 6.75 0 100 13.5A6.75 6.75 0 009 2.25zm0 3.5V10l2.5 1.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                    </svg>
                    MD
                    <svg slot="icon-right" viewBox="0 0 18 18" fill="none" aria-hidden="true">
                      <path d="M3.5 9h11M10 4.5l4.5 4.5-4.5 4.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </Button>
                  <Button size="lg" variant="dark">
                    <svg slot="icon-left" viewBox="0 0 20 20" fill="none" aria-hidden="true">
                      <path d="M10 2.5a7.5 7.5 0 100 15 7.5 7.5 0 000-15zm0 4V11l3 2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                    </svg>
                    LG
                    <svg slot="icon-right" viewBox="0 0 20 20" fill="none" aria-hidden="true">
                      <path d="M4 10h12M11 5l5 5-5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </Button>
                </div>
              </div>

              <!-- Только иконка (iconOnly) -->
              <div>
                <p class="text-xs text-gray-500 mb-3">Только иконка</p>
                <div class="flex flex-wrap gap-3 items-center">
                  <Button size="sm" variant="primary" iconOnly>
                    <svg slot="icon-left" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                      <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </Button>
                  <Button size="md" variant="primary" iconOnly>
                    <svg slot="icon-left" viewBox="0 0 18 18" fill="none" aria-hidden="true">
                      <path d="M3.5 9h11M10 4.5l4.5 4.5-4.5 4.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </Button>
                  <Button size="lg" variant="primary" iconOnly>
                    <svg slot="icon-left" viewBox="0 0 20 20" fill="none" aria-hidden="true">
                      <path d="M4 10h12M11 5l5 5-5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </Button>
                </div>
              </div>
            </div>
          </div>
        </div>
      )}

      {section === 'forms' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Формы</h1>
          <PropsTable props={formMeta.props} class="mb-8" />
          <div class="space-y-12">

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">stacked (по умолчанию) — лейбл сверху</p>
              <p class="text-xs text-gray-400 mb-4"><code>{`<Form preset="demo" />`}</code></p>
              <div class="max-w-sm border rounded-xl p-6">
                <Form preset="demo" />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">labels-left — лейбл слева, инпут справа</p>
              <p class="text-xs text-gray-400 mb-4"><code>{`<Form preset="demo" layout="labels-left" />`}</code></p>
              <div class="max-w-lg border rounded-xl p-6">
                <Form preset="demo" layout="labels-left" />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">inline — горизонтальная форма для hero-секций</p>
              <p class="text-xs text-gray-400 mb-4"><code>{`<Form preset="lead" layout="inline" />`}</code></p>
              <div class="border rounded-xl p-6 overflow-x-auto">
                <Form preset="lead" layout="inline" />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">inline + subscribe — только email</p>
              <p class="text-xs text-gray-400 mb-4"><code>{`<Form preset="subscribe" layout="inline" />`}</code></p>
              <div class="border rounded-xl p-6 max-w-md overflow-x-auto">
                <Form preset="subscribe" layout="inline" />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">inline + research — имя, email, компания</p>
              <p class="text-xs text-gray-400 mb-4"><code>{`<Form preset="research" layout="inline" />`}</code></p>
              <div class="border rounded-xl p-6 overflow-x-auto">
                <Form preset="research" layout="inline" />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">Пресеты</p>
              <div class="grid grid-cols-2 gap-6 max-w-2xl">
                <div class="border rounded-xl p-5">
                  <p class="text-xs text-gray-400 mb-3">preset="lead"</p>
                  <Form preset="lead" />
                </div>
                <div class="border rounded-xl p-5">
                  <p class="text-xs text-gray-400 mb-3">preset="subscribe"</p>
                  <Form preset="subscribe" />
                </div>
                <div class="border rounded-xl p-5">
                  <p class="text-xs text-gray-400 mb-3">preset="research"</p>
                  <Form preset="research" />
                </div>
              </div>
            </div>

          </div>
        </div>
      )}

      {section === 'form-closing' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">FormClosing</h1>
          <p class="text-gray-600 mb-4">
            Секция-завершение страницы. Два режима: <code>form</code> (поля + кнопка) и <code>buttons</code> (две кнопки вместо формы).
            Пункты <code>items</code> отображаются с иконкой check circle.
          </p>
          <PropsTable props={parseSource(formClosingRaw).props} class="mb-10" />

          <div class="space-y-12">

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">mode="buttons" — кнопки вместо формы (по Figma)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<FormClosing mode="buttons" title="..." items={[...]} primaryButton={{...}} secondaryButton={{...}} />`}</code></p>
              <div class="-mx-10">
                <FormClosing
                  mode="buttons"
                  title="Аудит вашего отдела продаж от аналитиков ДВИЖа"
                  items={[
                    'Выявим системные ошибки в процессах',
                    'Покажем быстрые точки роста',
                    'Предложим решения, которые подойдут именно вам',
                  ]}
                  primaryButton={{ label: 'Получить аудит', drawerPreset: 'demo' }}
                  secondaryButton={{ label: 'Скачать пример', drawerPreset: 'lead' }}
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">mode="form" с пунктами items (check circle)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<FormClosing preset="lead" title="..." items={['Пункт 1', 'Пункт 2']} />`}</code></p>
              <div class="-mx-10">
                <FormClosing
                  preset="lead"
                  title="Не оставим вас одних"
                  items={[
                    'Покажем, как решали задачи у клиентов',
                    'Рассчитаем ROMI и экономический эффект',
                    'Разберём ваши задачи',
                  ]}
                  submitLabel="Получить демо"
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">mode="form" базовый (description)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<FormClosing preset="lead" title="..." description="..." />`}</code></p>
              <div class="-mx-10">
                <FormClosing
                  preset="lead"
                  title="Попробуйте ДВИЖ в деле"
                  description="Оставьте заявку — свяжемся в течение часа."
                  submitLabel="Получить демо"
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">С фоновым изображением (тёмный вариант)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<FormClosing preset="demo" bgImage="/..." formVariant="dark" />`}</code></p>
              <div class="-mx-10">
                <FormClosing
                  preset="demo"
                  title="Запишитесь на демо"
                  description="Покажем платформу и ответим на все вопросы."
                  bgImage="https://admin.dvizh.cc/api/media/file/form-image.jpg"
                  formVariant="dark"
                  textColor="text-white"
                />
              </div>
            </div>

          </div>
        </div>
      )}


      {section === 'advantage-card' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">AdvantageCard</h1>
          <p class="text-gray-600 mb-8">{advantageCardMeta.description}</p>

          <PropsTable props={advantageCardMeta.props} class="mb-10" />

          <div class="space-y-10">
            {/* Базовые варианты */}
            <div>
              <h2 class="text-xl font-semibold mb-2">Базовые варианты</h2>
              <p class="text-gray-500 text-sm mb-4">Три карточки в сетке — стандартное использование на главной и продуктовых страницах.</p>
              <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <AdvantageCard
                  title="Закрывают до 26% больше сделок на том же трафике"
                  stat="+26%"
                  label="Сделок на том же трафике"
                />
                <AdvantageCard
                  title="Сокращают время обработки заявки в 3 раза"
                  stat="×3"
                  label="Быстрее обрабатывают заявки"
                />
                <AdvantageCard
                  title="Повышают конверсию ипотечных заявок"
                  stat="+18%"
                  label="Конверсия в выданные кредиты"
                />
              </div>
            </div>

            {/* Одиночная карточка */}
            <div>
              <h2 class="text-xl font-semibold mb-2">Одиночная карточка</h2>
              <p class="text-gray-500 text-sm mb-4">Ограниченная ширина — hover-эффект и клик по карточке открывают модальное окно.</p>
              <div class="max-w-sm">
                <AdvantageCard
                  title="Автоматизируют рутину и освобождают менеджеров для продаж"
                  stat="−40%"
                  label="Ручных операций"
                />
              </div>
            </div>

            {/* 2 колонки */}
            <div>
              <h2 class="text-xl font-semibold mb-2">2 колонки</h2>
              <p class="text-gray-500 text-sm mb-4">Симметричная сетка для коротких списков преимуществ.</p>
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 max-w-2xl">
                <AdvantageCard
                  title="Интеграции с ключевыми банками-партнёрами"
                  stat="50+"
                  label="Банков-партнёров"
                />
                <AdvantageCard
                  title="Крупнейших застройщиков выбрали ДВИЖ"
                  stat="75"
                  label="Из ТОП-100 застройщиков"
                />
              </div>
            </div>
          </div>
        </div>
      )}

      {section === 'drawer' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-8">Дровер</h1>
          <div class="space-y-10">

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">
                Глобальный дровер живёт в SiteLayout. Любая кнопка на странице открывает его через Alpine event:
              </p>
              <pre class="bg-gray-50 rounded-xl p-4 text-sm overflow-x-auto mb-6">{'@click="$dispatch(\'open-drawer\', { preset: \'demo\' })"'}</pre>
            </div>

            <div x-data="{ open: false, preset: 'demo' }">
              <p class="text-sm font-medium text-gray-400 mb-4">Демо — кнопки открывают разные пресеты</p>
              <div class="flex flex-wrap gap-3 mb-8">
                <button
                  @click="preset = 'demo'; open = true"
                  class="bg-black text-white text-sm font-medium px-4 py-2 rounded-lg hover:bg-gray-800"
                >
                  Запросить демо
                </button>
                <button
                  @click="preset = 'lead'; open = true"
                  class="border border-gray-900 text-sm font-medium px-4 py-2 rounded-lg hover:bg-gray-50"
                >
                  Связаться с нами
                </button>
                <button
                  @click="preset = 'research'; open = true"
                  class="border border-gray-900 text-sm font-medium px-4 py-2 rounded-lg hover:bg-gray-50"
                >
                  Получить исследование
                </button>
              </div>

              <!-- Inline drawer demo (самостоятельный, не глобальный) -->
              <div x-show="open" class="fixed inset-0 z-40 bg-black/30" @click="open = false" style="display:none"></div>
              <div
                x-show="open"
                x-transition:enter="transition ease-out duration-300"
                x-transition:enter-start="translate-x-full"
                x-transition:enter-end="translate-x-0"
                x-transition:leave="transition ease-in duration-200"
                x-transition:leave-start="translate-x-0"
                x-transition:leave-end="translate-x-full"
                class="fixed inset-y-0 right-0 z-50 w-full max-w-md bg-white shadow-xl flex flex-col"
                style="display:none"
              >
                <div class="flex items-center justify-between px-6 py-5 border-b border-gray-100 shrink-0">
                  <div>
                    <h2 x-show="preset === 'demo'" style="display:none" class="font-semibold">Запросить демо</h2>
                    <h2 x-show="preset === 'lead'" style="display:none" class="font-semibold">Связаться с нами</h2>
                    <h2 x-show="preset === 'research'" style="display:none" class="font-semibold">Получить исследование</h2>
                  </div>
                  <button @click="open = false" class="-mr-2 p-2 rounded-md text-gray-400 hover:text-gray-600">
                    <svg class="size-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12" />
                    </svg>
                  </button>
                </div>
                <div class="flex-1 overflow-y-auto px-6 py-6">
                  <div x-show="preset === 'demo'" style="display:none"><Form preset="demo" layout="labels-left" /></div>
                  <div x-show="preset === 'lead'" style="display:none"><Form preset="lead" layout="labels-left" /></div>
                  <div x-show="preset === 'research'" style="display:none"><Form preset="research" layout="labels-left" /></div>
                </div>
              </div>
            </div>

          </div>
        </div>
      )}

      {section === 'blocks' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">Блоки статей</h1>
          <p class="text-gray-600 mb-1">
            Добавляются в статью через CMS → BlogPosts → content (поле Rich Text с блоками).
          </p>
          <p class="text-sm text-gray-400 mb-2">
            Конфиги: <code>apps/cms/src/blocks/</code>
          </p>
          <p class="text-sm text-gray-400 mb-10">
            Рендереры: <code>apps/web/src/components/blocks/</code> → <code>BlockRenderer.astro</code>
          </p>

          <div class="space-y-16 max-w-2xl">

            <!-- RichTextBlock -->
            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">RichTextBlock — форматированный текст</p>
              <p class="text-xs text-gray-400 mb-4">
                blockType: <code>richText</code> · поле: <code>content</code> (Lexical JSON)
              </p>
              <div class="border rounded-xl p-6">
                <RichTextBlock block={{
                  content: {
                    root: {
                      children: [
                        { type: 'heading', tag: 'h2', children: [{ type: 'text', text: 'Заголовок внутри статьи', format: 0 }] },
                        { type: 'paragraph', children: [{ type: 'text', text: 'Основной текст. ДВИЖ помогает банкам и девелоперам автоматизировать ипотечный процесс от заявки до регистрации сделки.', format: 0 }] },
                        { type: 'list', listType: 'bullet', children: [
                          { type: 'listitem', children: [{ type: 'text', text: 'Электронная регистрация сделок', format: 0 }] },
                          { type: 'listitem', children: [{ type: 'text', text: 'Цифровая ипотека для банков', format: 0 }] },
                          { type: 'listitem', children: [{ type: 'text', text: 'Ипотечный калькулятор', format: 0 }] },
                        ]},
                      ]
                    }
                  }
                }} />
              </div>
            </div>

            <!-- CTABlock -->
            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">CTABlock — CTA-баннер</p>
              <p class="text-xs text-gray-400 mb-2">
                blockType: <code>cta</code> · поля: <code>heading</code>, <code>description</code>, <code>buttonText</code>, <code>buttonAction</code>, <code>formPreset</code> / <code>link</code>
              </p>
              <p class="text-xs text-gray-400 mb-4">
                buttonAction: <code>sidebarForm</code> — открывает дровер, <code>link</code> — переход по ссылке
              </p>
              <div class="space-y-4">
                <CTABlock block={{
                  heading: 'Попробуйте электронную регистрацию',
                  description: 'Покажем, как это работает для вашего банка или агентства недвижимости.',
                  buttonText: 'Запросить демо',
                  buttonAction: 'sidebarForm',
                  formPreset: 'demo',
                }} />
                <CTABlock block={{
                  heading: 'Читайте наши исследования',
                  buttonText: 'Перейти в блог',
                  buttonAction: 'link',
                  link: '/blog',
                }} />
              </div>
            </div>

            <!-- QuoteBlock -->
            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">QuoteBlock — цитата</p>
              <p class="text-xs text-gray-400 mb-4">
                blockType: <code>quote</code> · поля: <code>text</code>, <code>author</code>, <code>company</code>, <code>photo</code> (upload)
              </p>
              <div class="border rounded-xl px-6">
                <QuoteBlock block={{
                  text: 'ДВИЖ сократил время оформления ипотечной сделки с 14 дней до 2. Это кардинально изменило наш клиентский опыт.',
                  author: 'Иван Петров',
                  company: 'Банк "Надёжный"',
                }} />
              </div>
            </div>

            <!-- ImageBlock -->
            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">ImageBlock — изображение</p>
              <p class="text-xs text-gray-400 mb-4">
                blockType: <code>image</code> · поля: <code>image</code> (upload), <code>caption</code>, <code>alt</code>
              </p>
              <div class="border rounded-xl px-6">
                <ImageBlock block={{
                  image: { url: '' },
                  caption: 'Подпись к изображению',
                  alt: 'Демонстрационное изображение',
                }} />
              </div>
            </div>

            <!-- VideoBlock -->
            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">VideoBlock — видео (YouTube / Vimeo)</p>
              <p class="text-xs text-gray-400 mb-4">
                blockType: <code>video</code> · поля: <code>url</code> (YouTube или Vimeo), <code>caption</code>
              </p>
              <div class="border rounded-xl px-6">
                <VideoBlock block={{
                  url: 'https://www.youtube.com/watch?v=dQw4w9WgXcQ',
                  caption: 'Замените URL на реальное видео продукта',
                }} />
              </div>
            </div>

            <!-- FormBlock -->
            <div>
              <p class="text-sm font-medium text-gray-400 mb-1">FormBlock — форма внутри статьи</p>
              <p class="text-xs text-gray-400 mb-4">
                blockType: <code>form</code> · поля: <code>preset</code> (lead/subscribe/demo/research), <code>title</code>, <code>description</code>
              </p>
              <FormBlock block={{
                title: 'Получите исследование',
                description: 'Аналитика рынка ипотечного кредитования за 2024 год.',
                preset: 'research',
              }} />
            </div>

          </div>
        </div>
      )}
      {section === 'hero' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Hero Section</h1>
          <p class="text-gray-600 mb-4">
            Главная hero-секция страницы с заголовком, описанием, формой и опциональным изображением.
          </p>
          <PropsTable props={heroSectionMeta.props} class="mb-8" />

          <!-- Alpine Playground -->
          <div class="mb-12">
            <p class="text-xs font-semibold uppercase tracking-wider text-gray-400 mb-3">Playground</p>
            <div
              x-data="{
                layout: 'default',
                badge: 'Ипотечная платформа',
                title: 'Закрывайте до 26% больше сделок',
                description: 'Повышаем конверсии на каждом этапе работы с заявками.',
                withForm: true,
                formPreset: 'lead',
                demoLabel: 'Посмотреть демо',
                kpLabel: 'Получить КП'
              }"
              class="grid lg:grid-cols-[260px_1fr] border rounded-xl overflow-hidden"
            >
              <!-- Controls -->
              <div class="p-4 bg-gray-50 border-b lg:border-b-0 lg:border-r border-gray-200 space-y-3">
                <div>
                  <label class="text-xs text-gray-500 block mb-1">layout</label>
                  <select x-model="layout" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm bg-white">
                    <option value="default">default</option>
                    <option value="split">split</option>
                  </select>
                </div>
                <div>
                  <label class="text-xs text-gray-500 block mb-1">badge</label>
                  <input x-model="badge" type="text" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm" />
                </div>
                <div>
                  <label class="text-xs text-gray-500 block mb-1">title</label>
                  <input x-model="title" type="text" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm" />
                </div>
                <div>
                  <label class="text-xs text-gray-500 block mb-1">description</label>
                  <textarea x-model="description" rows="2" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm"></textarea>
                </div>
                <div x-show="layout === 'default'">
                  <div class="flex items-center gap-2">
                    <input x-model="withForm" type="checkbox" id="pg-hero-form" class="rounded border-gray-300" />
                    <label for="pg-hero-form" class="text-xs text-gray-500">withForm</label>
                  </div>
                  <div x-show="withForm" x-transition>
                    <label class="text-xs text-gray-500 block mb-1">formPreset</label>
                    <select x-model="formPreset" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm bg-white">
                      <option value="lead">lead</option>
                      <option value="demo">demo</option>
                      <option value="subscribe">subscribe</option>
                      <option value="research">research</option>
                    </select>
                  </div>
                </div>
                <div x-show="layout === 'split'">
                  <div>
                    <label class="text-xs text-gray-500 block mb-1">demoLabel</label>
                    <input x-model="demoLabel" type="text" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm" />
                  </div>
                  <div>
                    <label class="text-xs text-gray-500 block mb-1">kpLabel</label>
                    <input x-model="kpLabel" type="text" class="w-full border border-gray-300 rounded-md px-2 py-1.5 text-sm" />
                  </div>
                </div>
              </div>

              <!-- Preview -->
              <div class="bg-white overflow-hidden">
                <!-- Split layout preview -->
                <section x-show="layout === 'split'" class="bg-white">
                  <div class="max-w-[1440px] mx-auto px-4">
                    <div class="grid lg:grid-cols-2 gap-8 items-center py-16">
                      <div class="flex flex-col gap-6 order-2 lg:order-1">
                        <p x-show="badge" class="text-[18px] text-brand font-normal" x-text="badge"></p>
                        <h1 class="text-4xl font-bold leading-none text-[#1b2226]" x-text="title"></h1>
                      </div>
                      <div class="flex flex-col gap-6 order-1 lg:order-2">
                        <p x-show="description" class="text-lg text-[#858585] leading-[1.4]" x-text="description"></p>
                        <div class="flex gap-3">
                          <div class="px-4 py-2 rounded-lg bg-brand text-white text-sm" x-text="demoLabel"></div>
                          <div class="px-4 py-2 rounded-lg bg-gray-100 text-[#1b2226] text-sm" x-text="kpLabel"></div>
                        </div>
                      </div>
                    </div>
                  </div>
                </section>
                <!-- Default layout preview -->
                <section x-show="layout === 'default'" class="bg-white">
                  <div class="max-w-[1440px] mx-auto px-4">
                    <div class="grid lg:grid-cols-[1fr_1fr] gap-8 items-center min-h-[400px]">
                      <div class="flex flex-col justify-center gap-8 py-10">
                        <div class="flex flex-col gap-6">
                          <p x-show="badge" class="text-[18px] text-brand font-normal" x-text="badge"></p>
                          <div class="flex flex-col gap-5">
                            <h1 class="text-4xl font-medium leading-none text-[#1b2226]" x-text="title"></h1>
                            <p x-show="description" class="text-lg text-[#858585] leading-[1.4]" x-text="description"></p>
                          </div>
                        </div>
                        <div x-show="withForm" class="flex items-center gap-3 bg-gray-100 rounded-lg p-3">
                          <span class="text-xs text-gray-400">Форма</span>
                          <code class="text-xs font-mono text-gray-600" x-text="'preset=' + formPreset"></code>
                        </div>
                      </div>
                      <div class="h-[300px] bg-[#ededed] rounded-lg"></div>
                    </div>
                  </div>
                </section>
              </div>
            </div>
          </div>

          <div class="space-y-12">
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Полный вариант: бейдж + форма + изображение</p>
              <HeroSection
                badge="Ипотечная платформа"
                title="Закрывайте до 26% больше сделок на том же трафике"
                description="Повышаем конверсии на каждом этапе работы с заявками: от консультации до одобрения в банке"
                image=""
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">С формой demo (имя, email, телефон, компания)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<HeroSection formPreset="demo" ... />`}</code></p>
              <HeroSection
                title="Запросите демо"
                description="Покажем, как работает платформа."
                formPreset="demo"
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Без бейджа и изображения</p>
              <HeroSection
                title="Цифровая ипотека"
                description="Цифровизация ипотечного процесса для банков и девелоперов."
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Без формы (withForm={false})</p>
              <HeroSection
                badge="Продукт"
                title="Только заголовок и описание"
                description="Вариант без формы захвата."
                withForm={false}
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Split layout (Linear-style)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<HeroSection layout="split" />`}</code></p>
              <HeroSection
                layout="split"
                badge="Электронная регистрация"
                title="Регистрация сделок за 15 минут"
                description="Сократите затраты на регистрацию на 30%. Электронная подпись бесплатно."
                demoLabel="Посмотреть демо"
                kpLabel="Получить КП"
                stats={[
                  { value: 'на 30%', label: 'Сокращение затрат на регистрацию' },
                  { value: '15 мин', label: 'Подготовка пакета документов' },
                  { value: '0', label: 'Ошибок в документах' },
                  { value: '99.9%', label: 'Uptime сервиса' },
                ]}
                statsCols={4}
              />
            </div>
          </div>
        </div>
      )}
      {section === 'mega-menu' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">MegaMenu</h1>
          <p class="text-gray-600 mb-4">
            Мега-дропдаун навигации: 3 колонки секций (Продукты / Решения / Для кого) + колонка с 2 постами блога.
            Используется внутри <code class="text-sm bg-gray-100 px-1.5 py-0.5 rounded">NavBar.astro</code> — родитель управляет
            Alpine-состоянием <code class="text-sm bg-gray-100 px-1.5 py-0.5 rounded">megaOpen</code> и hover-зоной.
            Чтобы передать секции в NavBar — используйте поле <code class="text-sm bg-gray-100 px-1.5 py-0.5 rounded">sections</code>
            у элемента <code class="text-sm bg-gray-100 px-1.5 py-0.5 rounded">items</code> вместо <code class="text-sm bg-gray-100 px-1.5 py-0.5 rounded">children</code>.
          </p>
          <PropsTable props={megaMenuMeta.props} class="mb-8" />

          <div class="space-y-10">
            <div>
              <p class="text-xs font-semibold uppercase tracking-wider text-gray-400 mb-3">Standalone (megaOpen = true)</p>
              <div class="-mx-10 border-y border-gray-200 bg-background-primary" x-data="{ megaOpen: true }">
                <MegaMenu
                  sections={[
                    {
                      title: 'Продукты',
                      items: [
                        { label: 'Ипотека', href: '#', description: 'Процесс от заявки до выдачи' },
                        { label: 'Электронная регистрация', href: '#', description: 'Цифровая регистрация сделок' },
                        { label: 'QBR', href: '#', description: 'Аналитика и отчёты' },
                        { label: 'Скоринг', href: '#', description: 'AI-оценка заёмщика' },
                      ],
                    },
                    {
                      title: 'Решения',
                      items: [
                        { label: 'Для банков', href: '#' },
                        { label: 'Для девелоперов', href: '#' },
                        { label: 'Для агентств недвижимости', href: '#' },
                      ],
                    },
                    {
                      title: 'Для кого',
                      items: [
                        { label: 'Департамент продаж', href: '#' },
                        { label: 'Отдел ипотеки', href: '#' },
                        { label: 'Маркетинг', href: '#' },
                      ],
                    },
                  ]}
                  blogPosts={[
                    {
                      title: 'Как ИИ меняет работу отдела продаж застройщика',
                      slug: 'ai-sales',
                      category: 'Аналитика',
                    },
                    {
                      title: 'Электронная регистрация: что изменилось в 2026',
                      slug: 'e-reg-2026',
                      category: 'Продукт',
                    },
                  ]}
                />
              </div>
            </div>

            <div>
              <p class="text-xs font-semibold uppercase tracking-wider text-gray-400 mb-3">Без постов блога</p>
              <div class="-mx-10 border-y border-gray-200 bg-background-primary" x-data="{ megaOpen: true }">
                <MegaMenu
                  sections={[
                    {
                      title: 'Продукты',
                      items: [
                        { label: 'Ипотека', href: '#' },
                        { label: 'Электронная регистрация', href: '#' },
                        { label: 'QBR', href: '#' },
                      ],
                    },
                    {
                      title: 'Решения',
                      items: [
                        { label: 'Для банков', href: '#' },
                        { label: 'Для девелоперов', href: '#' },
                      ],
                    },
                    {
                      title: 'Для кого',
                      items: [
                        { label: 'Департамент продаж', href: '#' },
                        { label: 'Отдел ипотеки', href: '#' },
                      ],
                    },
                  ]}
                />
              </div>
            </div>
          </div>
        </div>
      )}

      {section === 'mesh-gradient' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Mesh Gradient</h1>
          <p class="text-gray-600 mb-4">
            Анимированный WebGL mesh-градиент (@paper-design/shaders). GPU-ускоренный, ~3-5KB. Автопауза когда не видно.
          </p>
          <PropsTable props={meshGradientMeta.props} class="mb-8" />

          <div class="space-y-8">
            <div>
              <p class="text-xs font-semibold uppercase tracking-wider text-gray-400 mb-3">Brand (default)</p>
              <div class="relative h-64 rounded-xl overflow-hidden">
                <MeshGradient />
              </div>
            </div>
            <div>
              <p class="text-xs font-semibold uppercase tracking-wider text-gray-400 mb-3">Warm orange-yellow</p>
              <div class="relative h-64 rounded-xl overflow-hidden">
                <MeshGradient colors={['#ff4d00', '#ff6b2c', '#ffb347', '#ffd866']} speed={0.3} />
              </div>
            </div>
            <div>
              <p class="text-xs font-semibold uppercase tracking-wider text-gray-400 mb-3">Slow, high distortion</p>
              <div class="relative h-64 rounded-xl overflow-hidden">
                <MeshGradient colors={['#ff4d00', '#ff8c42', '#1b2226', '#ffb347']} speed={0.15} distortion={0.9} swirl={0.4} />
              </div>
            </div>
          </div>
        </div>
      )}

      {section === 'timeline' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Timeline</h1>
          <p class="text-gray-600 mb-4">
            Блок с хронологией/историей. Год отображается шрифтом Styrene A, разделители между элементами.
          </p>
          <PropsTable props={timelineMeta.props} class="mb-10" />

          <div class="space-y-12">
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">С заголовком секции</p>
              <Timeline
                badge="История"
                title="История развития"
                items={[
                  { year: '2018', title: 'Цифровая ипотека', description: 'Начали с идеи — сделать ипотеку цифровой.' },
                  { year: '2020', title: 'Платформа ДВИЖ', description: 'Масштабировали продукт.' },
                  { year: '2024', title: 'Полностью онлайн', description: 'Первая в России ипотечная сделка полностью онлайн.' },
                ]}
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Без заголовка</p>
              <Timeline
                items={[
                  { year: 'Q1', title: 'Запуск MVP' },
                  { year: 'Q2', title: 'Первые клиенты', description: '10 девелоперов подключили платформу.' },
                  { year: 'Q3', title: 'Масштабирование' },
                ]}
              />
            </div>
          </div>
        </div>
      )}


      {section === 'blogcard' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Blog Card</h1>
          <p class="text-gray-600 mb-4">
            Карточка блог-поста. 4 размера: <code>lg</code> — горизонтальный большой, <code>md</code> — вертикальный (по умолчанию), <code>s</code> — горизонтальный компактный, <code>xs</code> — без обложки.
            Hover: заголовок → brand; у s/xs — фон bg-background-secondary.
          </p>
          <PropsTable props={blogCardMeta.props} class="mb-10" />

          <div class="space-y-16">

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">size="lg" — горизонтальный большой, обложка слева ~58%</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<BlogCard size="lg" title="..." slug="..." category="..." date="..." readTime="..." excerpt="..." />`}</code></p>
              <BlogCard
                size="lg"
                title="Как в 2 раза увеличить поток аудитории к концу 2026 года"
                slug="kak-uvelichit-potok"
                category="Маркетинг и продажи"
                date="14 апреля"
                readTime="13 мин"
                excerpt="Сценарии развития событий от Романа Гурова"
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">size="md" — вертикальный стандартный (по умолчанию)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<BlogCard size="md" title="..." slug="..." category="..." date="..." readTime="..." />`}</code></p>
              <div class="max-w-sm">
                <BlogCard
                  size="md"
                  title="«Главное изменение на рынке — скорость этих изменений»"
                  slug="glavnoe-izmenenie-na-rynke"
                  category="Аналитика рынка"
                  date="14 апреля"
                  readTime="13 мин"
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">size="s" — горизонтальный компактный, обложка 207px</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<BlogCard size="s" title="..." slug="..." category="..." date="..." readTime="..." />`}</code></p>
              <div class="max-w-2xl">
                <BlogCard
                  size="s"
                  title="Спасибо, что выжили, или как прошёл 2025 год для девелоперов"
                  slug="spasibo-chto-vyzhili"
                  category="Аналитика рынка"
                  date="14 апреля"
                  readTime="13 мин"
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">size="xs" — без обложки, только текст</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<BlogCard size="xs" title="..." slug="..." category="..." date="..." readTime="..." />`}</code></p>
              <div class="max-w-sm">
                <BlogCard
                  size="xs"
                  title="Уралсиб и ДВИЖ перешли на работу по протоколу API"
                  slug="uralsib-i-dvizh"
                  category="Новости"
                  date="14 апреля"
                  readTime="13 мин"
                />
              </div>
            </div>

          </div>
        </div>
      )}

      {section === 'blog-cards' && (
        <div>
          <h1 class="text-3xl font-bold mb-8">Blog Cards</h1>
          <p class="text-gray-600 mb-10">
            Сетка карточек статей блога с изображением, категорией, заголовком, описанием и автором. Базируется на компоненте из Tailwind UI.
          </p>

          <BlogCards
            articles={[
              {
                image: "https://images.unsplash.com/photo-1496128858413-b36217c2ce36?ixlib=rb-4.0.3&auto=format&fit=crop&w=3603&q=80",
                date: "Mar 16, 2020",
                category: "Marketing",
                title: "Boost your conversion rate",
                description: "Illo sint voluptas. Error voluptates culpa eligendi. Hic vel totam vitae illo. Non aliquid explicabo necessitatibus unde.",
                author: {
                  name: "Michael Foster",
                  role: "Co-Founder / CTO",
                  avatar: "https://images.unsplash.com/photo-1519244703995-f4e0f30006d5?ixlib=rb-1.2.1&auto=format&fit=facearea&facepad=2&w=256&h=256&q=80"
                }
              },
              {
                image: "https://images.unsplash.com/photo-1547586696-ea22b4d4235d?ixlib=rb-4.0.3&auto=format&fit=crop&w=3270&q=80",
                date: "Mar 10, 2020",
                category: "Sales",
                title: "How to use search engine optimization to drive sales",
                description: "Optio cum necessitatibus dolor voluptatum provident commodi et. Qui aperiam fugiat nemo cumque.",
                author: {
                  name: "Lindsay Walton",
                  role: "Front-end Developer",
                  avatar: "https://images.unsplash.com/photo-1517841905240-472988babdf9?ixlib=rb-1.2.1&auto=format&fit=facearea&facepad=2&w=256&h=256&q=80"
                }
              },
              {
                image: "https://images.unsplash.com/photo-1492724441997-5dc865305da7?ixlib=rb-4.0.3&auto=format&fit=crop&w=3270&q=80",
                date: "Feb 12, 2020",
                category: "Business",
                title: "Improve your customer experience",
                description: "Cupiditate maiores ullam eveniet adipisci in doloribus nulla minus. Voluptas iusto libero adipisci rem et corporis.",
                author: {
                  name: "Tom Cook",
                  role: "Director of Product",
                  avatar: "https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?ixlib=rb-1.2.1&auto=format&fit=facearea&facepad=2&w=256&h=256&q=80"
                }
              }
            ]}
          />
        </div>
      )}


      {section === 'island' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">Island</h1>
          <p class="text-gray-600 mb-8">Компактный pill-баннер с CTA-кнопкой. Тёмный фон, текст слева, оранжевая кнопка со стрелкой справа.</p>
          <PropsTable props={islandMeta.props} class="mb-8" />
          <div class="space-y-6">
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">С drawer</p>
              <Island
                label="Пора делать ДВИЖ"
                buttonText="Оставить заявку"
                drawerPreset="lead"
              />
            </div>
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Со ссылкой</p>
              <Island
                label="Пора делать ДВИЖ"
                buttonText="Узнать больше"
                buttonHref="/about"
              />
            </div>
          </div>
        </div>
      )}
      {section === 'modal-inline-form' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">Modal Inline Form</h1>
          <p class="text-gray-600 mb-8">
            Двухшаговая inline-форма. Шаг 1: имя + телефон. Шаг 2 (после отправки): email + компания.
            Три варианта лейаута: xs-sm / md / lg.
          </p>

          <div class="space-y-6">

            {/* xs-sm */}
            <div>
              <p class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-1">xs-sm — стек</p>
              <p class="text-xs text-gray-400 mb-3">Инпуты в столбик, кнопка во всю ширину</p>
              <div class="grid grid-cols-2 gap-4 max-w-2xl">
                <div class="bg-white border border-gray-200 rounded-2xl p-8 flex flex-col gap-4">
                  <p class="font-heading font-medium text-2xl text-gray-900 tracking-[-0.02em] leading-[1.2]">Попробуйте ДВИЖ в деле</p>
                  <div class="flex flex-col gap-2">
                    <Input name="name-xs" placeholder="Ваше имя" />
                    <Input name="phone-xs" placeholder="Телефон" />
                  </div>
                  <button class="w-full bg-brand text-white font-semibold text-base rounded-xl py-4 leading-[1.4] tracking-[-0.5px]">Получить демо</button>
                  <label class="flex items-start gap-2 cursor-pointer">
                    <input type="checkbox" class="mt-0.5 shrink-0 accent-brand" />
                    <span class="text-sm text-gray-400">Даю согласие на обработку персональных данных</span>
                  </label>
                </div>
                <div class="bg-white border border-gray-200 rounded-2xl p-8 flex flex-col gap-4">
                  <p class="font-heading font-medium text-2xl text-gray-900 tracking-[-0.02em] leading-[1.2]">Скоро свяжемся с вами!</p>
                  <div class="flex flex-col gap-2">
                    <Input name="email-xs" type="email" placeholder="Email" />
                    <Input name="company-xs" placeholder="Компания" />
                  </div>
                  <button class="w-full bg-brand text-white font-semibold text-base rounded-xl py-4 leading-[1.4] tracking-[-0.5px]">Получить демо</button>
                </div>
              </div>
            </div>

            {/* md */}
            <div>
              <p class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-1">md — 2 колонки</p>
              <p class="text-xs text-gray-400 mb-3">Инпуты в ряд, кнопка во всю ширину под ними</p>
              <div class="grid grid-cols-2 gap-4 max-w-3xl">
                <div class="bg-white border border-gray-200 rounded-2xl p-8 flex flex-col gap-4">
                  <p class="font-heading font-medium text-2xl text-gray-900 tracking-[-0.02em] leading-[1.2]">Попробуйте ДВИЖ в деле</p>
                  <div class="flex gap-2">
                    <Input name="name-md" placeholder="Ваше имя" class="flex-1 min-w-0" />
                    <Input name="phone-md" placeholder="Телефон" class="flex-1 min-w-0" />
                  </div>
                  <button class="w-full bg-brand text-white font-semibold text-sm rounded-xl py-3 leading-[1.4] tracking-[-0.5px]">Получить демо</button>
                  <label class="flex items-start gap-2 cursor-pointer">
                    <input type="checkbox" class="mt-0.5 shrink-0 accent-brand" />
                    <span class="text-sm text-gray-400">Даю согласие на обработку персональных данных</span>
                  </label>
                </div>
                <div class="bg-white border border-gray-200 rounded-2xl p-8 flex flex-col gap-4">
                  <p class="font-heading font-medium text-2xl text-gray-900 tracking-[-0.02em] leading-[1.2]">Скоро свяжемся с вами!</p>
                  <div class="flex gap-2">
                    <Input name="email-md" type="email" placeholder="Email" class="flex-1 min-w-0" />
                    <Input name="company-md" placeholder="Компания" class="flex-1 min-w-0" />
                  </div>
                  <button class="w-full bg-brand text-white font-semibold text-sm rounded-xl py-3 leading-[1.4] tracking-[-0.5px]">Получить демо</button>
                </div>
              </div>
            </div>

            {/* lg */}
            <div>
              <p class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-1">lg — один ряд</p>
              <p class="text-xs text-gray-400 mb-3">Инпуты + кнопка в одну строку, согласие под ними</p>
              <div class="flex flex-col gap-4">
                <div class="bg-white border border-gray-200 rounded-2xl p-8 flex flex-col gap-4">
                  <p class="font-heading font-medium text-2xl text-gray-900 tracking-[-0.02em] leading-[1.2]">Попробуйте ДВИЖ в деле</p>
                  <div class="flex gap-2 items-center">
                    <Input name="name-lg" placeholder="Ваше имя" class="flex-1 min-w-0" />
                    <Input name="phone-lg" placeholder="Телефон" class="flex-1 min-w-0" />
                    <button class="shrink-0 bg-brand text-white font-semibold text-sm rounded-xl px-6 py-[11px] leading-[1.4] tracking-[-0.5px]">Получить демо</button>
                  </div>
                  <label class="flex items-start gap-2 cursor-pointer">
                    <input type="checkbox" class="mt-0.5 shrink-0 accent-brand" />
                    <span class="text-sm text-gray-400">Даю согласие на обработку персональных данных</span>
                  </label>
                </div>
                <div class="bg-white border border-gray-200 rounded-2xl p-8 flex flex-col gap-4">
                  <p class="font-heading font-medium text-2xl text-gray-900 tracking-[-0.02em] leading-[1.2]">Скоро свяжемся с вами!</p>
                  <div class="flex gap-2 items-center">
                    <Input name="email-lg" type="email" placeholder="Email" class="flex-1 min-w-0" />
                    <Input name="company-lg" placeholder="Компания" class="flex-1 min-w-0" />
                    <button class="shrink-0 bg-brand text-white font-semibold text-sm rounded-xl px-6 py-[11px] leading-[1.4] tracking-[-0.5px]">Получить демо</button>
                  </div>
                </div>
              </div>
            </div>

          </div>

          {/* Живой компонент */}
          <div class="mt-12">
            <h2 class="text-xl font-semibold mb-2">Живой компонент</h2>
            <p class="text-gray-500 text-sm mb-4">Адаптивный — лейаут меняется по ширине вьюпорта</p>
            <div class="border border-gray-200 rounded-2xl bg-white p-8">
              <ModalInlineForm />
            </div>
          </div>
        </div>
      )}
      {section === 'side-menu' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">Side Menu</h1>
          <p class="text-gray-600 mb-8">Вертикальное боковое меню. Активный пункт выделяется оранжевым, переключается кликом.</p>
          <div class="space-y-8">
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">5 пунктов</p>
              <SideMenu items={[
                { title: 'Цифровая ипотека', active: true },
                { title: 'Ипотечный калькулятор' },
                { title: 'Финансовый скоринг' },
                { title: 'Электронная регистрация' },
                { title: 'ДВИЖ AI' },
              ]} />
            </div>
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">3 пункта</p>
              <SideMenu items={[
                { title: 'Для девелоперов', active: true },
                { title: 'Для банков' },
                { title: 'Для агентств' },
              ]} />
            </div>
          </div>
        </div>
      )}
      {section === 'story-card' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">StoryCard</h1>
          <p class="text-gray-600 mb-8">Карточка истории успеха с двумя вариантами оформления.</p>
          <div class="space-y-16">

            <PropsTable props={storyCardMeta.props} />

            {/* variant="active" */}
            <div>
              <h2 class="text-xl font-semibold mb-1">active</h2>
              <p class="text-gray-500 text-sm mb-6">Фоновое изображение, белый заголовок, тёмная тема PersonCard. Слот <code class="bg-gray-200 px-1 rounded text-xs">logo</code> — для логотипа компании.</p>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-[960px]">
                <div>
                  <p class="text-xs text-gray-400 mb-3">С логотипом</p>
                  <StoryCard
                    variant="active"
                    backgroundImage="https://images.unsplash.com/photo-1497366216548-37526070297c?q=80&w=1000&auto=format&fit=crop"
                    title="Помогли начать обрабатывать на 98% больше заявок"
                    person={{
                      name: "Светлана Корнелюк",
                      jobTitle: "Директор отдела продаж",
                      photo: { url: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?q=80&w=100&auto=format&fit=crop" }
                    }}
                  >
                    <div class="h-6 w-32 bg-white rounded flex items-center justify-center" slot="logo">
                      <span class="text-xs text-gray-800 font-bold">LOGO</span>
                    </div>
                  </StoryCard>
                </div>
                <div>
                  <p class="text-xs text-gray-400 mb-3">Без логотипа</p>
                  <StoryCard
                    variant="active"
                    backgroundImage="https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?q=80&w=1000&auto=format&fit=crop"
                    title="Увеличили конверсию сделок на 43% за 3 месяца"
                    person={{
                      name: "Алексей Громов",
                      jobTitle: "Руководитель отдела ипотеки",
                      photo: { url: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?q=80&w=100&auto=format&fit=crop" }
                    }}
                  />
                </div>
              </div>
            </div>

            {/* variant="default" */}
            <div>
              <h2 class="text-xl font-semibold mb-1">default</h2>
              <p class="text-gray-500 text-sm mb-6">Светлый фон <code class="bg-gray-200 px-1 rounded text-xs">bg-gray-100</code>, тёмный заголовок, светлая тема PersonCard.</p>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-[960px]">
                <div>
                  <p class="text-xs text-gray-400 mb-3">С логотипом</p>
                  <StoryCard
                    variant="default"
                    title="Помогли начать обрабатывать на 98% больше заявок"
                    person={{
                      name: "Светлана Корнелюк",
                      jobTitle: "Директор отдела продаж",
                      photo: { url: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?q=80&w=100&auto=format&fit=crop" }
                    }}
                  >
                    <div class="h-6 w-32 bg-gray-300 rounded flex items-center justify-center" slot="logo">
                      <span class="text-xs text-gray-800 font-bold">LOGO</span>
                    </div>
                  </StoryCard>
                </div>
                <div>
                  <p class="text-xs text-gray-400 mb-3">Без логотипа</p>
                  <StoryCard
                    variant="default"
                    title="Увеличили конверсию сделок на 43% за 3 месяца"
                    person={{
                      name: "Алексей Громов",
                      jobTitle: "Руководитель отдела ипотеки",
                      photo: { url: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?q=80&w=100&auto=format&fit=crop" }
                    }}
                  />
                </div>
              </div>
            </div>

          </div>
        </div>
      )}
      {section === 'input' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">Input</h1>
          <p class="text-gray-600 mb-8">{inputMeta.description}</p>

          <PropsTable props={inputMeta.props} class="mb-10" />

          <div class="space-y-12">

            {/* Размеры */}
            <div>
              <h2 class="text-xl font-semibold mb-1">Размеры</h2>
              <p class="text-gray-500 text-sm mb-6">M — 44px высота, S — компактный (py-2)</p>
              <div class="flex flex-col gap-4 max-w-xs">
                <div>
                  <p class="text-xs text-gray-400 mb-2">size="M" (default)</p>
                  <Input name="demo-m" placeholder="Placeholder" size="M" />
                </div>
                <div>
                  <p class="text-xs text-gray-400 mb-2">size="S"</p>
                  <Input name="demo-s" placeholder="Placeholder" size="S" />
                </div>
              </div>
            </div>

            {/* Состояния — Colour */}
            <div>
              <h2 class="text-xl font-semibold mb-1">Вариант colour</h2>
              <p class="text-gray-500 text-sm mb-6">Для светлого фона. Default / Active (фокус) / Filled (есть значение)</p>
              <div class="flex flex-col gap-4 max-w-xs">
                <div>
                  <p class="text-xs text-gray-400 mb-2">Default — нет фокуса, нет значения</p>
                  <Input name="c-default" placeholder="Placeholder" variant="colour" />
                </div>
                <div>
                  <p class="text-xs text-gray-400 mb-2">Active — кликни, чтобы увидеть оранжевую рамку</p>
                  <Input name="c-active" placeholder="Placeholder" variant="colour" />
                </div>
                <div>
                  <p class="text-xs text-gray-400 mb-2">Filled — есть значение</p>
                  <Input name="c-filled" placeholder="Placeholder" value="Info" variant="colour" />
                </div>
              </div>
            </div>

            {/* Состояния — White */}
            <div>
              <h2 class="text-xl font-semibold mb-1">Вариант white</h2>
              <p class="text-gray-500 text-sm mb-6">Для тёмного фона. При заполнении фон становится белым.</p>
              <div class="bg-gray-800 rounded-2xl p-6 max-w-xs flex flex-col gap-4">
                <div>
                  <p class="text-xs text-white/40 mb-2">Default</p>
                  <Input name="w-default" placeholder="Placeholder" variant="white" />
                </div>
                <div>
                  <p class="text-xs text-white/40 mb-2">Active — кликни для белой рамки</p>
                  <Input name="w-active" placeholder="Placeholder" variant="white" />
                </div>
                <div>
                  <p class="text-xs text-white/40 mb-2">Filled — белый фон</p>
                  <Input name="w-filled" placeholder="Placeholder" value="Info" variant="white" />
                </div>
              </div>
            </div>

            {/* Все комбинации */}
            <div>
              <h2 class="text-xl font-semibold mb-1">Все комбинации size × variant</h2>
              <p class="text-gray-500 text-sm mb-6">Аналог таблицы из Figma</p>
              <div class="grid grid-cols-2 gap-6 max-w-lg">
                <div class="flex flex-col gap-3">
                  <p class="text-xs font-semibold text-gray-400 uppercase tracking-wider">Colour / M</p>
                  <Input name="cm1" placeholder="Placeholder" variant="colour" size="M" />
                  <Input name="cm2" placeholder="Placeholder" value="Info" variant="colour" size="M" />
                </div>
                <div class="flex flex-col gap-3">
                  <p class="text-xs font-semibold text-gray-400 uppercase tracking-wider">Colour / S</p>
                  <Input name="cs1" placeholder="Placeholder" variant="colour" size="S" />
                  <Input name="cs2" placeholder="Placeholder" value="Info" variant="colour" size="S" />
                </div>
                <div class="bg-gray-800 rounded-xl p-3 flex flex-col gap-3">
                  <p class="text-xs font-semibold text-white/40 uppercase tracking-wider">White / M</p>
                  <Input name="wm1" placeholder="Placeholder" variant="white" size="M" />
                  <Input name="wm2" placeholder="Placeholder" value="Info" variant="white" size="M" />
                </div>
                <div class="bg-gray-800 rounded-xl p-3 flex flex-col gap-3">
                  <p class="text-xs font-semibold text-white/40 uppercase tracking-wider">White / S</p>
                  <Input name="ws1" placeholder="Placeholder" variant="white" size="S" />
                  <Input name="ws2" placeholder="Placeholder" value="Info" variant="white" size="S" />
                </div>
              </div>
            </div>

            {/* Типы */}
            <div>
              <h2 class="text-xl font-semibold mb-1">Типы (type)</h2>
              <p class="text-gray-500 text-sm mb-6">Поддерживаются все стандартные HTML-типы</p>
              <div class="flex flex-col gap-4 max-w-xs">
                <Input name="t-email" type="email" placeholder="Email" />
                <Input name="t-tel" type="tel" placeholder="Телефон" />
                <Input name="t-password" type="password" placeholder="Пароль" />
              </div>
            </div>

            {/* Disabled */}
            <div>
              <h2 class="text-xl font-semibold mb-1">Disabled</h2>
              <div class="flex flex-col gap-4 max-w-xs">
                <Input name="d-colour" placeholder="Отключённый colour" disabled />
                <div class="bg-gray-800 rounded-xl p-4">
                  <Input name="d-white" placeholder="Отключённый white" variant="white" disabled />
                </div>
              </div>
            </div>

          </div>
        </div>
      )}

      {section === 'bento' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Bento Grid</h1>
          <p class="text-gray-600 mb-4">
            Три карточки в bento-раскладке: два маленьких элемента слева (стек) + один большой справа.
            Карточки содержат заголовок, описание и опциональное изображение.
          </p>
          <PropsTable props={bentoGridMeta.props} class="mb-10" />

          <div class="space-y-12">
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">С заголовком секции</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<BentoGrid heading="..." items={[...3 items...]} />`}</code></p>
              <div class="-mx-10 bg-[#fbfbfb] px-10 py-12 rounded-2xl">
                <BentoGrid
                  heading="Другие причины выбрать ДВИЖ"
                  items={[
                    {
                      title: "Выпуск подписи за 5 минут",
                      description: "Поддерживаем все технологии: токен, Sign.Me, Госключ"
                    },
                    {
                      title: "Ускорим подачу заявления",
                      description: "Подключим оплату госпошлины с авансового счёта внутри ДВИЖа"
                    },
                    {
                      title: "Расчёт вероятности и скорости одобрения по банкам",
                      description: "Поможем зарегистрировать сложные сделки: с иностранными гражданами или военной ипотекой"
                    }
                  ]}
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Без заголовка секции</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<BentoGrid items={[...3 items...]} />`}</code></p>
              <div class="-mx-10 bg-[#fbfbfb] px-10 py-12 rounded-2xl">
                <BentoGrid
                  items={[
                    {
                      title: "Автоматизация заявок",
                      description: "Обрабатываем заявки без участия менеджера — 24/7"
                    },
                    {
                      title: "Интеграции с банками",
                      description: "Подключено более 20 банков-партнёров"
                    },
                    {
                      title: "Аналитика и отчёты в реальном времени",
                      description: "Мониторинг конверсий, отказов и скорости обработки на едином дашборде"
                    }
                  ]}
                />
              </div>
            </div>
          </div>
        </div>
      )}

      {section === 'product-card' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Product Card</h1>
          <p class="text-gray-600 mb-4">
            Большая hero-карточка продукта с фоновым паттерном и градиентным блюром.
          </p>
          <PropsTable props={productCardMeta.props} class="mb-10" />

          <div class="space-y-12">
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Полный вариант — заголовок + список + кнопка + изображение</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<ProductCard title="..." items={[...]} button={{...}} image={{...}} />`}</code></p>
              <ProductCard
                title="Поможем перевыполнить план продаж на том же трафике"
                items={[
                  "До 96% одобрения заявок",
                  "Единая анкета в 40+ банков",
                  "Моментальное заполнение через Госуслуги",
                  "Готовые интеграции с CRM",
                  "Анализ работы менеджеров"
                ]}
                button={{ label: "Подробнее о цифровой ипотеке", href: "#" }}
                image={{ src: "https://admin.dvizh.cc/api/media/file/product-section-1.jpg", alt: "Цифровая ипотека" }}
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Без картинки</p>
              <ProductCard
                title="Начните сегодня"
                items={["Закрывайте до 26% больше сделок на том же трафике"]}
                button={{ label: "Запросить демо", href: "#" }}
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">С отзывом клиента (prop review)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<ProductCard ... review={{ quote, author, role, company, caseLink }} />`}</code></p>
              <ProductCard
                title="Поможем перевыполнить план продаж на том же трафике"
                items={[
                  "До 96% одобрения заявок",
                  "Единая анкета в 40+ банков",
                  "Моментальное заполнение через Госуслуги",
                  "Готовые интеграции с CRM",
                ]}
                button={{ label: "Подробнее", href: "#" }}
                image={{ src: "https://admin.dvizh.cc/api/media/file/product-section-1.jpg", alt: "Цифровая ипотека" }}
                review={{
                  quote: "С появлением ДВИЖа мы ускорили подачу заявок с часа до 5 минут и минимизировали ошибки в заполнении анкет",
                  author: "Анастасия Швед",
                  role: "Руководитель управления прямых продаж",
                  company: { logo: "https://admin.dvizh.cc/api/media/file/talan-logo.svg", alt: "Талан" },
                  caseLink: { label: "Читать кейс", href: "#" },
                }}
              />
            </div>
          </div>
        </div>
      )}


      {section === 'product-section' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-6">Product Section New</h1>
          <p class="text-gray-600 mb-4">
            Sticky-меню слева + стек ProductCard справа. IntersectionObserver подсвечивает активный пункт. Клик — smooth scroll.
          </p>

          <ProductSection
            sections={[
              {
                number: '01',
                title: 'Цифровая ипотека',
                cardTitle: 'Поможем перевыполнить план продаж на том же трафике',
                items: ['До 96% одобрения', 'Единая анкета в 40+ банков', 'Моментальное заполнение через Госуслуги'],
                button: { label: 'Подробнее', href: '#' },
                image: { src: 'https://admin.dvizh.cc/api/media/file/product-section-1.jpg', alt: 'Цифровая ипотека' }
              },
              {
                number: '02',
                title: 'Ипотечный калькулятор',
                cardTitle: 'Приведем больше лидов с сайта',
                items: ['Расчет рассрочки и досрочного погашения', 'Автоматическое обновление ставок'],
                button: { label: 'Подробнее', href: '#' },
                image: { src: 'https://admin.dvizh.cc/api/media/file/product-section-2v2.jpg', alt: 'Калькулятор' }
              }
            ]}
          />
        </div>
      )}


      {section === 'feature-block' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">FeatureBlock</h1>
          <p class="text-gray-600 mb-8">
            Секция с авто-переключающимися пунктами слева (таймер + прогресс-бар) и изображением/слотом справа.
            Опционально: описание под заголовком и кнопка CTA.
          </p>
          <PropsTable props={parseSource(featureBlockRaw).props} class="mb-10" />

          <div class="space-y-16">

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">Базовый — только заголовок и пункты</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<FeatureBlock heading="..." features={[...]} />`}</code></p>
              <div class="-mx-10">
                <FeatureBlock
                  heading="Повышаем конверсию в&nbsp;2&nbsp;раза"
                  features={[
                    { title: '40+ банков-партнёров', subtitle: '15 «быстрых» банков по API' },
                    { title: '«Умный» скоринг покупателя', subtitle: 'До отправки в банк' },
                    { title: 'Встроенный ипотечный калькулятор', subtitle: 'На сайте застройщика' },
                    { title: 'Анкета «0 полей»', subtitle: 'Через Госуслуги' },
                    { title: 'Прямая связь с куратором', subtitle: 'На стороне банка' },
                    { title: 'Контроль «зависших» обращений 24/7' },
                  ]}
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">С описанием и кнопкой</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<FeatureBlock heading="..." description="..." button={{ label, drawerPreset }} features={[...]} />`}</code></p>
              <div class="-mx-10">
                <FeatureBlock
                  heading="Повышаем конверсию в&nbsp;2&nbsp;раза"
                  description="Подключите ДВИЖ и закрывайте до 26% больше сделок без увеличения затрат на маркетинг."
                  button={{ label: 'Узнать больше', drawerPreset: 'demo' }}
                  features={[
                    { title: '40+ банков-партнёров', subtitle: '15 «быстрых» банков по API' },
                    { title: '«Умный» скоринг покупателя', subtitle: 'До отправки в банк' },
                    { title: 'Встроенный ипотечный калькулятор', subtitle: 'На сайте застройщика' },
                    { title: 'Анкета «0 полей»', subtitle: 'Через Госуслуги' },
                    { title: 'Прямая связь с куратором', subtitle: 'На стороне банка' },
                    { title: 'Контроль «зависших» обращений 24/7' },
                  ]}
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">Только с описанием (без кнопки)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<FeatureBlock heading="..." description="..." features={[...]} />`}</code></p>
              <div class="-mx-10">
                <FeatureBlock
                  heading="Повышаем конверсию в&nbsp;2&nbsp;раза"
                  description="Подключите ДВИЖ и закрывайте до 26% больше сделок без увеличения затрат на маркетинг."
                  features={[
                    { title: '40+ банков-партнёров', subtitle: '15 «быстрых» банков по API' },
                    { title: '«Умный» скоринг покупателя' },
                    { title: 'Встроенный ипотечный калькулятор' },
                  ]}
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">Изображение слева (imageSide="left")</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<FeatureBlock imageSide="left" ... />`}</code></p>
              <div class="-mx-10">
                <FeatureBlock
                  heading="Повышаем конверсию в&nbsp;2&nbsp;раза"
                  description="Подключите ДВИЖ и закрывайте до 26% больше сделок."
                  button={{ label: 'Узнать больше', drawerPreset: 'demo' }}
                  imageSide="left"
                  features={[
                    { title: '40+ банков-партнёров', subtitle: '15 «быстрых» банков по API' },
                    { title: '«Умный» скоринг покупателя' },
                    { title: 'Встроенный ипотечный калькулятор' },
                  ]}
                />
              </div>
            </div>

          </div>
        </div>
      )}
      {section === 'success-story-block' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">SuccessStoryBlock</h1>
          <p class="text-gray-600 mb-8">
            Секция «истории успеха» с заголовком, кнопкой и сеткой из 3 карточек.
            Заголовок карточки поддерживает HTML — используй <code>&lt;span class="text-brand"&gt;</code> для оранжевых выделений.
          </p>
          <PropsTable props={parseSource(successStoryBlockRaw).props} class="mb-10" />

          <div class="-mx-10">
            <SuccessStoryBlock
              heading="70+ историй успеха на рынке застройщиков"
              buttonLabel="Получить демо"
              buttonPreset="demo"
              stories={[
                {
                  logo: '/partners/lsr.svg',
                  logoAlt: 'Группа ЛСР',
                  titleHtml: 'Помогли начать обрабатывать <span class="text-brand">на 281% заявок больше,</span> чем в среднем на рынке',
                  personName: 'Группа ЛСР',
                  personJobTitle: 'Один из ведущих строительных холдингов',
                },
                {
                  logo: '/partners/strana.svg',
                  logoAlt: 'ГК Точно',
                  titleHtml: 'Увеличили объём закрытых сделок <span class="text-brand">с 1165 до 3500 за год</span>',
                  personName: 'ГК «Точно»',
                  personJobTitle: 'Крупнейший девелопер Краснодарского края',
                },
                {
                  logo: '/partners/a101.svg',
                  logoAlt: 'ГК Самолет',
                  titleHtml: 'Дополнительно продали <span class="text-brand">96 квартир</span>',
                  personName: 'ГК «Самолет»',
                  personJobTitle: 'Крупнейший федеральный девелопер России',
                },
              ]}
            />
          </div>
        </div>
      )}

      {section === 'logo-grid' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">LogoGrid</h1>
          <p class="text-gray-600 mb-8">
            Бегущая строка логотипов клиентов/партнёров (marquee). Бесшовный цикл из двух наборов.
          </p>
          <PropsTable props={parseSource(logoGridRaw).props} class="mb-10" />

          <p class="text-xs text-gray-400 mb-4">Логотипы намеренно серые и полупрозрачные (grayscale 100%, opacity 45%) — так по дизайну.</p>
          <div class="bg-white rounded-2xl overflow-hidden -mx-10">
            <LogoGrid
              logos={[
                { src: '/partners/dark/lsr.svg', alt: 'ЛСР' },
                { src: '/partners/dark/a101.svg', alt: 'А101' },
                { src: '/partners/dark/ava.svg', alt: 'AVA' },
                { src: '/partners/dark/glorax.svg', alt: 'Глоракс' },
                { src: '/partners/dark/ganel.svg', alt: 'Гранель' },
                { src: '/partners/dark/strana.svg', alt: 'Страна' },
              ]}
            />
          </div>
        </div>
      )}

      {section === 'key-facts' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">KeyFacts</h1>
          <p class="text-gray-600 mb-4">
            Нумерованные факты/метрики с вертикальной чертой. Заголовок опционален.
            2–4 колонки. Контент «прибит» к низу каждой колонки.
          </p>
          <PropsTable props={parseSource(keyFactsRaw).props} class="mb-10" />

          <div class="space-y-12">
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">3 колонки с заголовком</p>
              <KeyFacts
                title="Зачем вам ипотечный калькулятор?"
                items={[
                  { value: '80% заявок', description: 'приходит в нерабочее время,<br>не теряйте их' },
                  { value: '50% клиентов', description: 'заранее хотят рассчитать ежемесячный платеж' },
                  { value: 'на 20% выше', description: 'конверсия в сделку, когда клиент знает доступный бюджет ипотеки' },
                ]}
              />
            </div>
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">4 колонки без заголовка</p>
              <KeyFacts
                cols={4}
                items={[
                  { value: '1,5 млн', description: 'заявок обработано через платформу' },
                  { value: '75 из 100', description: 'крупнейших застройщиков России используют ДВИЖ' },
                  { value: '40+ банков', description: 'подключено к единой системе скоринга' },
                  { value: 'до 10 сек', description: 'заполнение ипотечной анкеты клиентом' },
                ]}
              />
            </div>
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">2 колонки</p>
              <KeyFacts
                cols={2}
                title="Цифры говорят сами за себя"
                items={[
                  { value: '99,9% uptime', description: 'гарантированная доступность сервиса' },
                  { value: '< 200 мс', description: 'среднее время ответа API' },
                ]}
              />
            </div>
          </div>
        </div>
      )}

      {section === 'comparison' && (
        <div class="space-y-16">
          <div>
            <h1 class="text-3xl font-bold font-heading mb-6">ComparisonSection</h1>
            <p class="text-gray-600 mb-4">
              Блок сравнения: левая (серая) колонка — рыночные решения, правая (тёмная) — ДВИЖ с CTA-кнопками.
            </p>
            <PropsTable props={parseSource(comparisonSectionRaw).props} class="mb-10" />
          </div>

          {/* Вариант default */}
          <div>
            <p class="text-sm font-medium text-gray-400 mb-4">variant="default" — пункты с иконками</p>
            <div class="-mx-10">
              <ComparisonSection
                label="В чём разница"
                title="Выбирайте лучшее"
                leftTitle="Самописные и рыночные решения"
                leftItems={[
                  'Необходимо вручную поддерживать актуальность ставок',
                  'Нельзя рассчитать комбо- и ипотеку с удорожанием стоимости',
                  'Нет возможности выделить наиболее выгодное предложение',
                  'Нет потенциала для апсейла клиентов',
                ]}
                rightItems={[
                  'Ипотечные ставки обновляются автоматически',
                  'Считает все виды ипотек: траншевую, комбо, с удорожанием, субсидированную, рассрочку',
                  'Меняет порядок ипотечных программ и показывает выгодные предложения',
                  'Увеличивает средний чек за счёт персональных предложений',
                ]}
                primaryButtonText="Записаться на демо"
                secondaryButtonText="Пробовать бесплатно"
              />
            </div>
          </div>

          {/* Вариант prices */}
          <div>
            <p class="text-sm font-medium text-gray-400 mb-4">variant="prices" — ценовое сравнение</p>
            <div class="-mx-10">
              <ComparisonSection
                variant="prices"
                label="Стоимость"
                title="Дешевле на каждом этапе"
                leftTitle="Без ДВИЖа"
                priceRows={[
                  { label: 'Регистрация', leftValue: 'от 1 700 ₽', rightValue: '1 000 ₽' },
                  { label: 'Госпошлина', leftValue: '7%', rightValue: '200 ₽' },
                ]}
                class="py-24"
              />
            </div>
          </div>

          {/* Вариант prices + CTA слот */}
          <div>
            <p class="text-sm font-medium text-gray-400 mb-4">variant="prices" + слот cta — CTABanner внутри с маленьким отступом</p>
            <div class="-mx-10">
              <ComparisonSection
                variant="prices"
                label="Стоимость"
                title="Дешевле на каждом этапе"
                leftTitle="Без ДВИЖа"
                priceRows={[
                  { label: 'Регистрация', leftValue: 'от 1 700 ₽', rightValue: '1 000 ₽' },
                  { label: 'Госпошлина', leftValue: '7%', rightValue: '200 ₽' },
                ]}
                class="py-24"
              >
                <CTABanner
                  slot="cta"
                  title="Узнайте, сколько вы можете сэкономить"
                  buttonText="Получить расчёт"
                  drawerPreset="lead"
                />
              </ComparisonSection>
            </div>
          </div>

        </div>
      )}




      {section === 'text-image-block' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">TextImageBlock</h1>
          <p class="text-gray-600 mb-8">
            Секция с текстом слева (заголовок + подзаголовок + кнопка) и изображением справа.
            Правая колонка — слот <code>image</code>: передай любой <code>&lt;img&gt;</code> или иллюстрацию.
            Без слота — карточка остаётся пустым плейсхолдером.
          </p>
          <PropsTable props={textImageBlockMeta.props} class="mb-10" />

          <h2 class="text-xl font-semibold mb-4">С плейсхолдером (без изображения)</h2>
          <p class="text-xs text-gray-400 mb-6">
            <code>{`<TextImageBlock heading="..." subheading="..." buttonText="..." drawerPreset="demo" />`}</code>
          </p>
          <div class="mb-10 -mx-4 sm:-mx-6 lg:-mx-10">
            <TextImageBlock
              heading="Заберите готовое решение и одобряйте больше клиентов"
              subheading="Наш скоринг заменяет самописные модели — и поднимает одобрение без роста риска."
              buttonText="Запросить демо"
              drawerPreset="demo"
            />
          </div>

          <h2 class="text-xl font-semibold mb-4">С изображением в слоте</h2>
          <p class="text-xs text-gray-400 mb-6">
            <code>{`<TextImageBlock ...><img slot="image" src="..." class="w-full h-full object-cover" /></TextImageBlock>`}</code>
          </p>
          <div class="mb-10 -mx-4 sm:-mx-6 lg:-mx-10">
            <TextImageBlock
              heading="Заберите готовое решение и одобряйте больше клиентов"
              subheading="Наш скоринг заменяет самописные модели — и поднимает одобрение без роста риска."
              buttonText="Забрать решение"
              buttonHref="/scoring"
            >
              <div slot="image" class="w-full h-full flex items-center justify-center bg-gray-200 text-gray-400 text-sm">
                Здесь будет ваше изображение
              </div>
            </TextImageBlock>
          </div>
        </div>
      )}


      {section === 'support-section' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">SupportSection</h1>
          <p class="text-gray-600 mb-8">
            Блок поддержки с тремя интерактивными табами. Клик по плашке справа меняет фото и данные слева (crossfade).
          </p>
          <PropsTable props={parseSource(supportSectionRaw).props} class="mb-10" />

          <div class="-mx-4 sm:-mx-6 lg:-mx-10">
            <SupportSection
              tabs={[
                {
                  title: '15 консультантов поддержки',
                  bullets: [
                    'Решат технический вопрос прямо в чате',
                    'Доступны 24/7 — в рабочий день и в момент важной сделки',
                    'Знают продукт изнутри и дают конкретные ответы',
                  ],
                  personName: 'Третьякова Юлия',
                  personTitle: 'Руководитель отдела ипотеки и финансовых инструментов',
                  avatars: ['', '', ''],
                },
                {
                  title: '7 менеджеров успеха',
                  bullets: [
                    'Настроят платформу под процессы вашего отдела продаж',
                    'Проведут онбординг команды',
                    'Подскажут, какие функции вы ещё не используете',
                  ],
                  personName: 'Иванов Сергей',
                  personTitle: 'Руководитель направления Customer Success',
                  avatars: ['', '', '', ''],
                },
                {
                  title: '8 аналитиков',
                  bullets: [
                    'Разберут вашу воронку и найдут, где теряются заявки',
                    'Покажут, кто из менеджеров тормозит сделки и почему',
                    'Дадут конкретные рекомендации — не выводы, а следующий шаг',
                  ],
                  personName: 'Козлова Анна',
                  personTitle: 'Руководитель аналитического отдела',
                  avatars: ['', ''],
                },
              ]}
            />
          </div>
        </div>
      )}

      {section === 'blog-grid' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">BlogGrid</h1>
          <p class="text-gray-600 mb-8">
            Сетка постов блога с фильтром по категориям и пагинацией.
            Используется в <code>/blog</code>, <code>/category/[slug]</code>, <code>/blog/page/[page]</code>.
            Реальные данные приходят из Payload CMS — здесь демо с одной заглушечной карточкой.
          </p>
          <PropsTable props={blogGridMeta.props} class="mb-10" />

          <div class="-mx-4">
            <BlogGrid
              posts={[
                { title: 'Демо-статья — заглушка для UI Kit', slug: '#', cover: null, publishedAt: '2026-01-01', excerpt: 'Краткое описание поста для предпросмотра карточки.', category: { title: 'Аналитика', slug: 'analytics' }, person: { name: 'Иван Иванов', slug: 'ivan' } }
              ]}
              allPosts={[1]}
              categories={[{ title: 'Аналитика', slug: 'analytics' }, { title: 'Продажи', slug: 'sales' }]}
              categoryCountMap={{ analytics: 1, sales: 0 }}
              currentPage={1}
              totalPages={1}
              activeCategory={undefined}
            />
          </div>
        </div>
      )}

      {section === 'banner' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">Banner</h1>
          <p class="text-gray-600 mb-8">
            Универсальный CTA-баннер. Части включаются булевыми пропсами — как в Figma:
            подзаголовок, список пунктов, встроенная форма, боковая кнопка.
            Фон — любой Tailwind-класс или изображение. Скругление: 24px / 16px.
          </p>
          <PropsTable props={bannerMeta.props} class="mb-10" />

          <div class="space-y-8">

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Компактный — заголовок + боковая кнопка (замена CTABanner), rounded="2xl"</p>
              <Banner
                title="Узнайте, сколько вы можете сэкономить"
                showSideButton
                buttonText="Получить расчёт"
                buttonDrawerPreset="lead"
                rounded="2xl"
                bg="bg-background-secondary"
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">С подзаголовком и боковой кнопкой, светлый фон (default)</p>
              <Banner
                title="Автоматизируйте продажи"
                subtitle="AI-платформа для застройщиков — 75 из ТОП-100 крупнейших девелоперов России"
                showSideButton
                buttonText="Запросить демо"
                buttonDrawerPreset="demo"
                bg="bg-background-secondary"
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Со списком пунктов и боковой кнопкой</p>
              <Banner
                title="Почему выбирают ДВИЖ"
                subtitle="Всё для роста продаж в одной платформе"
                paragraphs={[
                  'До 96% одобрения ипотечных заявок',
                  'Единая анкета в 40+ банков',
                  'Готовые интеграции с CRM',
                ]}
                showSideButton
                buttonText="Узнать больше"
                buttonDrawerPreset="demo"
                bg="bg-background-secondary"
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">С встроенной формой и сноской</p>
              <Banner
                title="Получите бесплатный расчёт"
                subtitle="Заполните форму — менеджер свяжется с вами в течение часа"
                showForm
                formPreset="lead"
                formSubmitLabel="Рассчитать"
                formFootnote="Нажимая кнопку, вы соглашаетесь с политикой конфиденциальности"
                bg="bg-background-secondary"
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">Тёмный фон, bg-gray-900 + dark</p>
              <Banner
                title="Запросить демо"
                subtitle="Покажем, как ДВИЖ помогает автоматизировать ипотеку и увеличить конверсию"
                paragraphs={[
                  'Настройка под ваш CRM за 1 день',
                  'Персональный менеджер',
                ]}
                showSideButton
                buttonText="Запросить демо"
                buttonDrawerPreset="demo"
                bg="bg-gray-900"
                dark
              />
            </div>

          </div>
        </div>
      )}

      {section === 'cta' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">CTA</h1>
          <p class="text-gray-600 mb-8">
            Призыв к действию. Варианты: <code>dark</code> (тёмный фон) или <code>light</code> (светлый).
            Кнопка может вести по <code>buttonHref</code> или открывать дровер через <code>drawerPreset</code>.
          </p>
          <PropsTable props={ctaMeta.props} class="mb-10" />

          <div class="space-y-12">
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">variant="dark"</p>
              <CTA
                title="Запросить демо"
                description="Покажем, как ДВИЖ поможет вашему бизнесу"
                buttonText="Запросить демо"
                drawerPreset="demo"
                variant="dark"
              />
            </div>
            <div>
              <p class="text-sm font-medium text-gray-400 mb-4">variant="light"</p>
              <CTA
                title="Запросить демо"
                description="Покажем, как ДВИЖ поможет вашему бизнесу"
                buttonText="Запросить демо"
                drawerPreset="demo"
                variant="light"
              />
            </div>
          </div>
        </div>
      )}

      {section === 'cta-banner' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">CTABanner</h1>
          <p class="text-gray-600 mb-8">
            Горизонтальная плашка CTA: текст слева, кнопка справа. Скруглённые углы, светлый фон.
            Используется как компактный призыв внутри секций.
          </p>
          <PropsTable props={ctaBannerMeta.props} class="mb-10" />

          <div class="space-y-8">
            <CTABanner
              title="Узнайте, сколько вы можете сэкономить"
              buttonText="Получить расчёт"
              drawerPreset="lead"
            />
            <CTABanner
              title="Подключите ДВИЖ за 14 дней"
              buttonText="Запросить демо"
              drawerPreset="demo"
            />
          </div>
        </div>
      )}

      {section === 'clear-glass-bg' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">ClearGlassBg</h1>
          <p class="text-gray-600 mb-8">
            WebGL-эффект «прозрачное рифлёное стекло» поверх изображения в родителе.
            0 зависимостей, один draw call. Активируется всегда, по hover родителя или через событие.
          </p>
          <PropsTable props={clearGlassBgMeta.props} class="mb-10" />

          <div class="relative overflow-hidden h-80 rounded-2xl">
            <img
              src="https://images.unsplash.com/photo-1497366216548-37526070297c?q=80&w=1600&auto=format&fit=crop"
              alt="Background"
              class="absolute inset-0 w-full h-full object-cover"
            />
            <ClearGlassBg />
          </div>
        </div>
      )}

      {section === 'developer-block' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">DeveloperBlock</h1>
          <p class="text-gray-600 mb-8">
            Секция с авто-переключающимися пунктами слева и изображением справа.
            Активный пункт раскрывает подпись и показывает прогресс-бар (переключение каждые 5 сек).
          </p>
          <PropsTable props={developerBlockMeta.props} class="mb-10" />

          <div class="-mx-4 sm:-mx-6 lg:-mx-10">
            <DeveloperBlock
              heading="Повышаем конверсию в 2 раза"
              features={[
                { title: '40+ банков-партнёров', subtitle: '15 «быстрых» банков по API' },
                { title: '«Умный» скоринг покупателя', subtitle: 'До отправки в банк' },
                { title: 'Встроенный ипотечный калькулятор', subtitle: 'На сайте застройщика' },
                { title: 'Анкета «0 полей»', subtitle: 'Через Госуслуги' },
              ]}
            />
          </div>
        </div>
      )}

      {section === 'fluted-glass-bg' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">FlutedGlassBg</h1>
          <p class="text-gray-600 mb-8">
            Анимированный фон: mesh-градиент + fluted glass в одном WebGL-шейдере.
            Drop-in замена MeshGradient — тот же API позиционирования (<code>absolute, inset: 0</code>).
            Автопауза при выходе из viewport и при prefers-reduced-motion.
          </p>
          <PropsTable props={flutedGlassBgMeta.props} class="mb-10" />

          <div class="relative overflow-hidden h-80 rounded-2xl">
            <FlutedGlassBg />
          </div>
        </div>
      )}

      {section === 'funnel-section' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">FunnelSection</h1>
          <p class="text-gray-600 mb-8">
            Продукты по этапам сделки в формате воронки. Слева — название этапа, справа — карточки ProductLink.
            Вертикальная линия с кружками-индикаторами связывает этапы.
          </p>
          <PropsTable props={funnelSectionMeta.props} class="mb-10" />

          <div class="-mx-4 sm:-mx-6 lg:-mx-10">
            <FunnelSection
              heading="Добиваемся высоких конверсий на каждом этапе сделки"
              groups={[
                {
                  label: 'Лидогенерация',
                  cards: [
                    { title: 'Ипотечный калькулятор', description: 'Собирайте заявки в нерабочее время', href: '/ipotechnyy-kalkulyator' },
                    { title: 'Личный кабинет клиента', description: 'Объедините офлайн и онлайн продажи', href: '/lichnyy-kabinet' },
                  ],
                },
                {
                  label: 'Управление заявками',
                  cards: [
                    { title: 'Цифровая ипотека', description: 'Одобряйте до 95% заявок', href: '/ipoteka' },
                    { title: 'Финансовый скоринг', description: 'Сократите число отказов', href: '/scoring' },
                  ],
                },
                {
                  label: 'Регистрация',
                  cards: [
                    { title: 'Электронная регистрация', description: 'Сделки любого типа и сложности', href: '/elektronnaya-registraciya' },
                  ],
                },
              ]}
            />
          </div>
        </div>
      )}

      {section === 'logo-drum' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">LogoDrum</h1>
          <p class="text-gray-600 mb-8">
            3D-барабан с логотипами (чистый CSS). Логотипы вращаются вокруг оси X,
            каждый проходит через «фокусную зону». Ноль JavaScript.
          </p>
          <PropsTable props={logoDrumMeta.props} class="mb-10" />

          <div class="h-[500px] bg-gray-900 rounded-2xl overflow-hidden">
            <LogoDrum
              logos={['/partners/a101.svg', '/partners/ava.svg', '/partners/ganel.svg', '/partners/glorax.svg', '/partners/lsr.svg', '/partners/strana.svg']}
              duration={35}
            />
          </div>
        </div>
      )}

      {section === 'page-section' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">PageSection</h1>
          <p class="text-gray-600 mb-8">
            Секция с навигацией слева и контентом справа. Слева — список разделов с активным состоянием,
            справа — заголовок и список преимуществ.
          </p>
          <PropsTable props={pageSectionMeta.props} class="mb-10" />

          <PageSection
            title="Поможем перевыполнить план продаж на том же трафике"
            benefits={[
              'До 96% одобрения ипотечных заявок',
              'Единая анкета в 40+ банков',
              'Моментальное заполнение через Госуслуги',
              'Готовые интеграции с CRM',
            ]}
            sections={[
              { title: 'Цифровая ипотека', active: true },
              { title: 'Ипотечный калькулятор' },
              { title: 'Финансовый скоринг' },
              { title: 'Электронная регистрация' },
              { title: 'ДВИЖ AI' },
            ]}
          />
        </div>
      )}

      {section === 'product-link' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">ProductLink</h1>
          <p class="text-gray-600 mb-8">
            Ссылка на продукт: иконка 60×60 + заголовок + описание.
            Hover делает текст оранжевым. Используется внутри FunnelSection.
          </p>
          <PropsTable props={productLinkMeta.props} class="mb-10" />

          <div class="space-y-8 max-w-xl">
            <ProductLink
              title="Ипотечный калькулятор"
              description="Собирайте заявки в нерабочее время"
              href="/ipotechnyy-kalkulyator"
            />
            <ProductLink
              title="Электронная регистрация"
              description="Сделки онлайн без визита в МФЦ"
              href="/elektronnaya-registraciya"
            />
            <ProductLink
              title="Финансовый скоринг"
              description="Оценка заёмщика до отправки в банк"
              href="/scoring"
            />
          </div>
        </div>
      )}

      {section === 'review' && (
        <div>
          <h1 class="text-3xl font-bold font-heading mb-2">Review</h1>
          <p class="text-gray-600 mb-8">
            Блок цитаты клиента. Без <code>photo</code> — компактная цитата (используется внутри ProductCard).
            С <code>photo</code> — на lg+ фото слева, на мобайле только цитата.
          </p>
          <PropsTable props={reviewMeta.props} class="mb-10" />

          <div class="space-y-12">

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">Без фото — полный вариант (default)</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<Review quote="..." author="..." role="..." company={{...}} caseLink={{...}} />`}</code></p>
              <div class="bg-stone-100 rounded-[32px] p-8 lg:p-10">
                <Review
                  quote="С появлением ДВИЖа мы ускорили подачу заявок с часа до 5 минут и минимизировали ошибки в заполнении анкет"
                  author="Анастасия Швед"
                  role="Руководитель управления прямых продаж"
                  company={{ logo: 'https://admin.dvizh.cc/api/media/file/talan-logo.svg', alt: 'Талан' }}
                  caseLink={{ label: 'Читать кейс', href: '#' }}
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">Без фото — без логотипа</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<Review quote="..." author="..." role="..." />`}</code></p>
              <div class="bg-stone-100 rounded-[32px] p-8 lg:p-10">
                <Review
                  quote="Интеграция с ДВИЖем заняла неделю, а ROI превысил ожидания уже в первый месяц"
                  author="Елена Козлова"
                  role="Коммерческий директор"
                />
              </div>
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">С фото — lg+: фото слева, mobile: только цитата</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<Review photo={{src:"...", alt:"..."}} quote="..." author="..." role="..." company={{...}} caseLink={{...}} />`}</code></p>
              <Review
                photo={{ src: '', alt: 'Бурганов Рунар' }}
                quote="Разработка отдельного инструмента под каждый проект — это большие трудозатраты и финансовые расходы. За счёт стороннего решения мы оптимизируем процессы и сокращаем сроки реализации."
                author="Бурганов Рунар"
                role="Руководитель отдела ипотеки и финансовых инструментов"
                company={{ logo: 'https://admin.dvizh.cc/api/media/file/kortros-logo.svg', alt: 'Кортрос' }}
                caseLink={{ label: 'Читать кейс', href: '#' }}
              />
            </div>

            <div>
              <p class="text-sm font-medium text-gray-400 mb-2">С фото — без кнопки и логотипа</p>
              <p class="text-xs text-gray-400 mb-6"><code>{`<Review photo={{src:"...", alt:"..."}} quote="..." author="..." role="..." />`}</code></p>
              <Review
                photo={{ src: '', alt: 'Елена Козлова' }}
                quote="Интеграция с ДВИЖем заняла неделю, а ROI превысил ожидания уже в первый месяц"
                author="Елена Козлова"
                role="Коммерческий директор"
              />
            </div>

          </div>
        </div>
      )}

    </main>
  </div>
</BaseLayout>
